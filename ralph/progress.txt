# Ralph Progress Log
Started: Sun Apr 19 15:32:14 PDT 2026

## Codebase Patterns
- SQLAlchemy 2.0 declarative base lives at `server/src/database.py` (`Base`, `engine`, `SessionLocal`, `get_database_url`). All models import `Base` from there.
- ORM models live at `server/src/models/<name>.py`; `server/src/models/__init__.py` is the single import point and re-exports `Base` plus every model so Alembic's `env.py` can register metadata via one import.
- Alembic is configured at `server/alembic.ini` with `prepend_sys_path = %(here)s/..` so migrations always run from repo root: `alembic -c server/alembic.ini upgrade head`.
- `env.py` pulls the DB URL from `DATABASE_URL` env var (default `sqlite:///./server_dev.db`) and sets `render_as_batch=True` for SQLite so schema changes remain portable.
- Absolute imports (`from server.src.<pkg>`) are mandatory — requires `server/__init__.py` + `server/src/__init__.py` present. Relative `from src...` breaks mypy's dual-namespace check.
- Enum-like fields use VARCHAR + `CheckConstraint("col IN (...)")` for Postgres/SQLite portability, and values are re-exported as a module-level tuple (e.g. `SESSAO_STATUS_VALUES`) for reuse in API layers.
- Mypy config at repo root `mypy.ini` covers `server/src` + `server/alembic` with strict-ish defaults (`disallow_untyped_defs = True`). Run `mypy` from repo root.
- FastAPI app object is at `server/src/main.py:app`; boot with `uvicorn server.src.main:app --reload` from repo root.
- Pydantic v2 schemas live at `server/src/schemas/<entity>.py`. Use `EmailStr` (requires `email-validator`) for emails and `ConfigDict(from_attributes=True)` on response models so SQLAlchemy ORM rows convert via `Model.model_validate(orm_obj)`.
- HTTP routers live at `server/src/routers/<name>.py` and are wired into `server/src/main.py` via `app.include_router(<module>.router)`. Use `APIRouter(prefix="/api/<resource>", tags=[...])`.
- DB-bound endpoints inject the session with `SessionDep = Annotated[Session, Depends(get_session)]` — keeps mypy clean (no `Depends()` default-value type mismatch).
- Password hashing helpers at `server/src/security.py` — `hash_password(senha)` / `verify_password(senha, senha_hash)` use bcrypt's `gensalt()`/`checkpw`. Always hash before persisting; never store plaintext.
- Smoke-test FastAPI routers without spawning uvicorn: build a `TestClient(app)` over an in-memory SQLite engine and override `get_session` via `app.dependency_overrides`. In-memory SQLite **must** use `poolclass=StaticPool` + `connect_args={"check_same_thread": False}`, otherwise each connection sees an empty schema.
- JWT auth: `server/src/security.py` has `create_access_token(subject)` / `decode_access_token(token)` using `HS256` + `SECRET_KEY` env var (dev default present). `server/src/auth.py` exposes `get_current_user` dependency and the `CurrentUser = Annotated[Usuario, Depends(get_current_user)]` alias — protect routes with `def route(user: CurrentUser, ...)`. The dependency uses `OAuth2PasswordBearer(auto_error=False)` so all 401 responses share a single uniform body (no leak of "missing token" vs "bad token").
- Heavy ML deps (`cv2`, `ultralytics`) are isolated in `server/src/video_pipeline.py` behind the `VideoValidator` protocol; `get_video_validator()` is the FastAPI dependency, overridable in tests. Lazy-import `cv2`/`ultralytics` inside methods so importing the module never loads YOLO. `mypy.ini` has `ignore_missing_imports = True` for `cv2.*`, `ultralytics.*`, `numpy.*`.
- Multipart form uploads need `python-multipart` (in `requirements.txt`). Use `Annotated[float, Form()]` / `Annotated[UploadFile, File()]` on endpoint params; persist to `UPLOAD_DIR` (env var, default `./uploads/`, gitignored) and call `_cleanup(path)` on any validation failure before re-raising `HTTPException`.
- Dispatch long-running work with `background_tasks: BackgroundTasks` parameter (FastAPI built-in) + `background_tasks.add_task(fn, ...)`; tasks run after the response is sent so the client is not blocked. The pipeline entrypoint convention is `server.src.video_pipeline.run_pipeline(sessao_id, video_path)`.
- Enforce per-user access on resource routes by loading the row (`session.get(Model, id)`) then: `None → 404`, `row.usuario_id != user.id → 403`. Do not leak existence by collapsing to 404 for both — the tests (and PRD) require a distinct 403.
- Background pipeline runs **outside** FastAPI's dependency-injection scope, so `app.dependency_overrides` does not affect it. `server.src.video_pipeline.run_pipeline` takes optional `extractor` (`PoseExtractor` protocol) and `session_factory` kwargs — injection points for tests. The HTTP call site keeps the positional signature `run_pipeline(sessao_id, video_path)`.
- Heavy-pipeline protocols follow the same lazy pattern as `VideoValidator`: `cv2`/`ultralytics` imports live inside method bodies, never at module top, so `import server.src.video_pipeline` never loads YOLO.
- COCO keypoint filtering: `KEYPOINT_SCORE_THRESHOLD = 0.5` — below that, set keypoint to `None` (treated as NaN downstream). A frame is "low quality" when ≥ `LOW_QUALITY_KP_THRESHOLD` (= 9, majority of 17) of its keypoints are None. Reuse these constants from `video_pipeline` for any downstream biomechanics code.
- `smooth_frames(frames, window=5)` applies a moving-average on x,y per keypoint, **preserving None** — it never interpolates over a dropped frame. Any new keypoint-consuming story should accept the smoothed series (post-suavização) as input, not raw extractor output.
- Biomechanics utilities live at `server/src/biomechanics/<metric>.py`; each module re-exports its public API via `server/src/biomechanics/__init__.py`. The package is the single import point for downstream stories (US-008 onward) and for `run_pipeline` when it wires up metric persistence.
- Unit tests for biomechanics go in `server/tests/test_<metric>.py` (pytest). Run with `python -m pytest server/tests/` from repo root. `server/tests` is included in `mypy.ini` so test helpers must be fully typed (same strictness as `server/src`).
- Domain errors from biomechanics utilities raise `ValueError` with a module-level `MSG_*` constant as the message, so callers can match on identity and tests use `pytest.raises(ValueError, match=re.escape(MSG_*))`.
- Métricas calculadas por `run_pipeline` são gravadas em `METRICA` via funções privadas `_persistir_<metric>(session, sessao_id, frames)` (lazy-import do módulo `biomechanics` + `Metrica` dentro da função para manter `video_pipeline` leve). Não alteram `sessao.status` — a transição final para `concluido` pertence a US-016 (gerador de recomendações).
- Convenção de ângulo do joelho no **backend** (US-008+) é o ângulo interno (perna estendida = 180°), diferente da convenção `180-internal` do legado em `processing/src/mainGraph.py`. Ao tocar qualquer módulo biomecânico novo, mantenha a convenção interna (arccos do produto escalar normalizado).
- Detecção de contato inicial: picos locais estritos de Y do tornozelo (helper privado em `biomechanics/joelho.py`). Bordas nunca são pico. Reutilize esta ideia para US-010 (cadência) e US-013 (TCS).
- Métricas por-lado **sem ciclos** (ângulo médio de cotovelo, etc.) usam o shape `AnguloXxxLado(angulo_medio_graus, frames_validos)` — campo `frames_validos: int` em vez de `frames_contato: tuple[int, ...]`. Reserve `frames_contato` para métricas de ciclo (joelho no contato, TCS, overstriding).
- Cálculos dependentes de tempo real (cadência, TCS) recebem `fps` como parâmetro explícito de `calcular_<metric>(frames, fps)` e levantam `ValueError(MSG_FPS_INVALIDO)` para `fps <= 0`. Os helpers `_persistir_<metric>` em `video_pipeline.py` repassam `result.fps` — a camada biomecânica nunca lê `sessao.fps` nem toca o ORM.
- Convenção de "flexão" em filtros do PRD: `flexao_graus = 180 - angulo_interno`. As métricas que filtram por fase de apoio (tronco em US-011, futuras métricas de TCS) traduzem internamente as faixas do PRD ("flexão ~40°–45°") para o ângulo interno backend. Constantes `FLEXAO_APOIO_MEDIO_{MIN,MAX}_GRAUS` em `biomechanics/tronco.py` são reutilizáveis.
- Métricas com sinal (inclinação do tronco) preservam direção via convenção: positivo ⇒ +X (frente para corredor andando para a direita), negativo ⇒ -X. A fórmula `arctan(ΔX/ΔY)` do PRD é aplicada como `arctan(ΔX / -ΔY_imagem)` para compensar o eixo Y crescente para baixo nas coordenadas de imagem; ΔY_imagem ≥ 0 (tronco horizontal/invertido) resulta em frame descartado.
- Métricas que precisam de `fator_escala` (overstriding em US-012, oscilação vertical em US-014) recebem o `fator_escala: float` como parâmetro puro; é o `_persistir_<metric>(session, sessao_id, frames, altura_cm)` que carrega `usuario.altura_cm` (de `sessao.usuario`), chama `calcular_fator_escala(frames, altura_cm).fator_escala` e repassa. A camada biomecânica nunca lê o ORM nem `usuario.altura_cm` direto; falhas do fator (altura None/≤0 ou nenhum frame válido para altura_pixels) são engolidas no helper persist (no-op silencioso) — não interrompem o pipeline.
- `_find_initial_contact_indices` + `_ankle_y` agora estão duplicados em três módulos (`joelho.py`, `cadencia.py`, `overstriding.py`). US-013 (TCS) será a quarta cópia — neste ponto vale extrair para `biomechanics/_picos.py` e reexportar dos módulos atuais. Adiar até US-013 para manter cada história isolada (refatorações cross-module devem ter sua própria iteração).
- TCS (US-013) usa detecção de ciclo diferente das demais métricas de contato: **plateau é aceito** (`Y[i-1] < Y[i]` e `Y[i+1] ≤ Y[i]`) e o fim do ciclo é a primeira queda estrita (`Y[j] < Y[j-1]`). O helper privado `_detectar_ciclos_contato(y_series)` retorna `(inicio_idx, frames_de_contato)`. Isso é incompatível com `_find_initial_contact_indices` (picos estritos sem plateau) — uma refatoração cross-module futura precisa expor os dois modos (pico estrito vs. plateau) em vez de substituir o atual por um só.
- Métricas informativas (`apenas_informativa=True`) já acumulam duas histórias: cadência (US-010) e TCS (US-013). US-016 (nota geral) deve filtrar em bloco todas as métricas com esse flag para evitar penalização — não o fazer case-a-case por `tipo`.
- Métricas de **ciclo de passada** (oscilação vertical em US-014) usam contatos consecutivos do **pé direito** como bordas de ciclo (mesma convenção da cadência por PRD Seção 6.3). Por ciclo, o intervalo de frames é fechado `[i, j]` (inclui ambos os contatos). Quando a métrica precisa de N contatos para formar N-1 ciclos, o guard correto é `len(contatos) < 2 → None`.
- Índice de simetria (`indice_simetria(esq, dir)`) em `biomechanics/simetria.py` é a única fórmula reutilizável esq/dir do PRD: `|esq-dir| / ((esq+dir)/2) × 100`. Retorna `None` para lado ausente (erro controlado) e `0.0` quando `esq == dir == 0` (degenerado). `calcular_simetria(frames, fps, fator_escala)` degrada graciosamente: `fps <= 0 ⇒ simetria_tcs=None`; `fator_escala is None or <= 0 ⇒ simetria_oscilacao=None`. Reutilizável para qualquer métrica futura esq/dir (overstriding, ângulos de cotovelo) se US-016/US-025 pedir.
- Gerador de recomendações (`biomechanics/recomendacoes.py`) é agnóstico a keypoints: recebe apenas `list[tuple[str, float]]` (tipo, valor) e devolve `AnaliseResultado(nota_geral, feedback_ia, recomendacoes)`. O dict de dispatch `_ANALISADORES` mapeia cada `tipo` (coluna `Metrica.tipo`) à função de análise — novas métricas, quando adicionadas, só precisam registrar a entrada no dict + função `_classificar_*`/`_analisar_*`. Tipos desconhecidos são silenciosamente ignorados (NO-OP). Pesos: `PESO_ATENCAO=0.5`, `PESO_CRITICO=1.5`, `NOTA_MAXIMA=10.0` — ajustar aqui reflete em toda a nota.
- `_finalizar_analise(session, sessao_id)` em `video_pipeline.py` é o **único** ponto que transita `sessao.status` para `concluido`. Demais `_persistir_<metric>` jamais tocam o status. Ele carrega todas as `Metrica` da sessão, filtra `apenas_informativa=True` em Python (sem SQL `where == False` para manter portabilidade), e grava `Recomendacao` + atualiza `nota_geral` + `feedback_ia` + `status`. Endpoints que leem esses campos (US-017, US-019, US-025) devem tratá-los como populados apenas quando `status='concluido'`.
- Endpoints de análise (sob prefixo `/api/analysis`) vivem em `server/src/routers/analysis.py`. Controle de acesso segue o mesmo padrão de vídeos: `session.get(SessaoAnalise, id)` → 404 se `None`, 403 se `usuario_id != user.id`. Para status de erro (`erro_qualidade_keypoints`, `erro_multiplas_pessoas`), responder 200 com um campo `erro` descritivo (usar `STATUS_DESCRICAO` de `routers/videos.py` como fonte única da mensagem) e listas vazias para `recomendacoes`/`metricas_resumidas`. Schemas de resposta (`RecomendacaoResponse`, `MetricaResumida`) usam `ConfigDict(from_attributes=True)` para conversão direta de ORM rows via `Model.model_validate(orm_obj)`.
- Dados brutos por-frame (para `/api/analysis/{id}/raw`) são serializados como JSON em `SessaoAnalise.dados_brutos_json` (TEXT, nullable) pelo helper `_persistir_dados_brutos` em `video_pipeline.py`. A serialização acontece **antes** dos demais `_persistir_<metric>`, logo após o status transitar para `calculando_metricas`; não é gravada em caminhos de erro (`erro_qualidade_keypoints`/`erro_multiplas_pessoas`). Valores ausentes (keypoints com score < 0.5, métricas per-frame sem dados) viram `null` na serialização — nunca `NaN` (uso de `json.dumps(..., allow_nan=False)`).
- Métricas **per-frame** (ângulo joelho/cotovelo/tronco + y_com) vivem em `server/src/biomechanics/frame_metrics.py` separadas das agregadas (média, por-ciclo, etc.). Convenção: cada função recebe um único `FrameKeypoints` + lado (quando aplicável) e retorna `float | None`. Per-frame não aplica filtros contextuais (ex: `inclinacao_tronco_frame` não filtra por apoio médio — vale para qualquer frame com os quatro keypoints disponíveis). Reaproveite estes helpers para US-026 (gráficos por frame) e para qualquer história futura que precise de séries temporais sem agregação.
- Tests de roteadores FastAPI seguem o padrão `server/tests/test_<router>_<endpoint>.py` com fixtures pytest: `session_factory` (in-memory SQLite + StaticPool) e `client` (TestClient com `app.dependency_overrides[get_session]`). Autenticação nos testes usa `create_access_token(user_id)` direto — não chama `/api/auth/login`. Limpeza de `dependency_overrides` é responsabilidade da fixture `client` (pop no teardown).
- Frontend vive em `client/` (Vite + React 19 + TS 6, scaffolding `create-vite --template react-ts`). Typecheck da história = `npm run build` (roda `tsc -b` + `vite build`) executado a partir de `client/`. `tsconfig.app.json` tem `verbatimModuleSyntax: true` (sempre `import type { ... }` para tipos) e `erasableSyntaxOnly: true` (nada de `enum`/`namespace`; use objetos `as const`). Estrutura: `src/api/` (axios + chamadas), `src/context/` (`AuthContext`), `src/components/` (`PrivateRoute`), `src/pages/<Tela>Page.tsx`.
- Cliente HTTP do frontend: singleton axios em `client/src/api/client.ts` com base URL de `import.meta.env.VITE_API_BASE_URL` (fallback `http://localhost:8000`). Interceptor global lê `localStorage[TOKEN_STORAGE_KEY]` e injeta `Authorization: Bearer <token>`. Não adicione headers manualmente nas chamadas; chame `api.post(...)` / `api.get(...)` direto.
- Autenticação do frontend: `AuthProvider` em `client/src/context/AuthContext.tsx` + hook `useAuth()` exposto `{ user, token, isAuthenticated, login, signup, logout }`. `login({email, senha})` bate no `POST /api/auth/login`; `signup(payload)` faz register + login automático e popula o perfil em `user`. JWT decodificado manualmente (sem lib externa) para extrair `sub` como `user.id`. Token persistido em localStorage e hidratado no construtor do provider.
- Rotas privadas no frontend: envolva o `element` da rota em `<PrivateRoute>` (vive em `client/src/components/PrivateRoute.tsx`) — quando `!isAuthenticated`, renderiza `<Navigate to="/login" replace state={{ from: location.pathname }} />`. US-021+ podem consumir `state.from` via `useLocation()` para retornar à rota original após login. Path de fallback `*` já redireciona para `/login`.
- Helper unificado para erros de API do axios: `extractApiError(err)` em `client/src/api/errors.ts` retorna `{ general: string | null, fields: Record<string, string> }`. Lida com FastAPI 422 (array de `{loc, msg}` → map por nome de campo), 401, 409, `detail` string e erros de rede. Use em todo formulário/fluxo que chama a API (upload em US-023, qualquer POST futuro).
- Páginas já autenticadas redirecionam com `<Navigate to={redirectTo} replace />` dentro do próprio componente (não em `useEffect`), espelhando o padrão do `PrivateRoute`. Isso evita flash de conteúdo da página de login ao recarregar com token válido.
- Estilo de formulário: classes `auth-container`, `auth-form`, `.field`, `.field-error`, `.form-error` já vivem em `client/src/index.css`. Reutilize-as em formulários novos (US-023 upload, etc.) em vez de criar CSS local por página.
- Testes do frontend usam Vitest (`npm test` → `vitest run`, ambiente Node, sem JSDOM). Utilitários puros vivem em `client/src/utils/<nome>.ts` com testes em `client/src/utils/<nome>.test.ts` ao lado. Para componentes que precisem de DOM no futuro, instale `jsdom` + configure `test.environment: 'jsdom'` em `vite.config.ts`.
- Validações DOM-dependent no frontend (vídeo, futuras integrações com Web APIs) seguem padrão split: lógica pura em `src/utils/<nome>Validation.ts` (testável em Node) + reader/parser DOM em `src/utils/<nome>Meta.ts` (sem teste unitário, validação manual no browser). Constantes de threshold e mensagens vivem na camada pura para reuso/teste.
- Uploads multipart no frontend usam `FormData` em `src/api/<recurso>.ts` (ex: `videos.ts#uploadVideoRequest`). Nunca defina `Content-Type` manualmente — browser+axios anexa boundary automaticamente. O interceptor de auth global injeta o `Authorization: Bearer` sem interferir.
- Polling de endpoints (ex: status de processamento em US-024) segue padrão `setTimeout` auto-reagendado dentro de `useEffect`: cada resposta agenda a próxima chamada; `useRef<number | null>` guarda o timer id; `cancelledRef = useRef(false)` é marcado `true` na cleanup e checado antes de cada `setState`/reagendamento. Estados finais param o polling. Mapeamento status→UI vive em `src/utils/videoStatus.ts` (puro, testável): `STATUS_*`, `STATUS_STAGES`, `progressPercentFromStatus`, `isErrorStatus`, `errorMessageForStatus`, `isFinalStatus`. Reutilizável em US-027 (histórico).
- Gráficos de séries temporais no frontend usam **Recharts 3** (`client/dependencies.recharts`). Padrão: `ResponsiveContainer` + `LineChart` + `Line` por série, dados convertidos em `FramePoint[]` (objetos com `frame_idx` + chaves da série) via `buildFramePoints()` em `client/src/utils/rawAnalysis.ts`. Para aplicar transforms consistentes com a UI (ex: joelho interno→flexão), declare-os em `CHART_SPECS`. Ausentes (`null`/NaN) viram `null` — `<Line connectNulls={false}/>` deixa gaps no gráfico (não interpola). `isAnimationActive={false}` evita animação custosa em séries longas. Reutilizável para US-027 ou qualquer história futura que plote séries por frame/tempo.
- Paginação client-side via **query string** (`?page=X`): `useSearchParams` do react-router é a fonte única — estado derivado com `parsePageParam(searchParams.get('page'))`. Navegação atualiza `setSearchParams(nextParams)`; refresh preserva posição. Formatters compartilhados de histórico vivem em `client/src/utils/historicoDisplay.ts` (`statusBadge`, `formatPaceMinKm`, `formatNotaGeral`, `formatCriadoEm`, `totalPages`). Reutilizáveis para futuras listagens paginadas (ex: comparação de sessões).
- App shell e branding (US-029): nome do produto em `client/src/branding.ts` (`BRAND_NAME = 'Stride'`); títulos de página via `usePageTitle('Tela')` (`client/src/hooks/usePageTitle.ts`). Toda rota privada é envolvida em `<PrivateRoute><AppShell>{page}</AppShell></PrivateRoute>` via helper `privateElement()` em `App.tsx` — Login/Signup ficam fora. AppShell renderiza top nav sticky 64px (logo + nav + avatar com dropdown em ≥640px; hamburger drawer em <640px) → `<div className="app-shell-main">` (wrapper não-semântico; página é dona do `<main>`) → footer. Drawer mobile usa `inert` em `#shell-root` quando aberto e devolve foco ao botão hamburger no close. Z-index: header=100, user-menu=110, backdrop=200, drawer=210.
- Acessibilidade base (US-030): o skip-link (`<a className="skip-link" href="#main">`) vive em `App.tsx` fora de `<Routes>` — primeiro focável em qualquer rota. Cada página é dona do seu `<main id="main" tabIndex={-1}>` como raiz do JSX (inclusive Login/Signup); NUNCA aninhar `<main>` dentro do AppShell. Estilos globais em `client/src/index.css`: `*:focus-visible { outline:none; box-shadow: var(--shadow-focus) }`, `@media (prefers-reduced-motion: reduce)` neutraliza transitions/animations em `*`, `h1..h6 { scroll-margin-top: var(--space-16) }`, `main[tabindex]:focus { outline:none }` (evita outline visível após skip-link). Proibido `outline:none` sem replacement visível. Botões icon-only exigem `aria-label` em pt-BR; setas/ícones decorativos ficam `aria-hidden="true"`. Exatamente **um `<h1>` por página**.
- Biblioteca de componentes compartilhados (US-031): `client/src/components/ui/` (Button, Input, Field, Card, Badge, Banner, Skeleton) com barrel em `index.ts`. Todos consomem exclusivamente tokens CSS e classes globais `ui-*` em `index.css`. Convenções: `import type` em todo tipo (verbatimModuleSyntax), props usando `HTMLAttributes<Element>` quando há spread natural (Button, Input), `useId()` para ids implícitos (Field). Spinner do Button é inline e mantém o label (não substitui). Input aplica `font-size: 16px` até 639px e cai para `--text-sm` em desktop (evita iOS zoom sem perder hierarquia visual). Reutilize esta biblioteca em qualquer redesign de página (US-035+) em vez de replicar markup/CSS. **Página migration fica para as histórias seguintes** — a US-031 só cria a biblioteca.
- Sistema de toasts (US-032): `ToastProvider` em `client/src/context/ToastContext.tsx` + hook `useToast()` expõe `toast.success/error/info/dismiss`. Portal em `document.body` no canto inferior-direito (stack reverso, mais novo em cima); duas regiões `aria-live`: `polite` (success/info) e `assertive` (error). Auto-dismiss em 5s; hover/focus pausa o timer e retoma com o tempo restante no mouseleave. Animação de entrada via `transform: translateX + opacity` (classe `toast-enter` adicionada no primeiro `requestAnimationFrame` após o mount). `prefers-reduced-motion` global em `index.css` já neutraliza a transition. Mensagem global de 401 (sessão expirada) é disparada via `setUnauthorizedHandler` em `api/client.ts`: o `UnauthorizedBridge` (montado dentro de `AuthProvider`+`BrowserRouter`+`ToastProvider`) registra um handler que chama `toast.error('Sessão expirada') + logout() + navigate('/login')`. O interceptor pula o handler quando o request atingiu `/api/auth/login` ou `/api/users/register` (para não derrubar o usuário em credenciais inválidas). Para disparar um toast em sucesso/falha de chamadas pontuais, use o hook `useToast()` dentro do componente; para erros globais (não ligados a um campo específico), sempre toast — **proibido** `<p className="form-error">` solto fora de `<form>`. Em erros de estado de página (identificador ausente, `data.erro` do backend) use `<Banner variant="danger" assertive>` da biblioteca UI.
- Estados padronizados (US-033): `LoadingState | EmptyState | ErrorState` em `client/src/components/ui/states/` (reexportados via `components/ui/index.ts`). `LoadingState variant="status|analysis|analysis-raw|historico"` renderiza skeletons com dimensões fixas espelhando o layout final (CLS ~0). Páginas com fetch pontual expõem retry via `retryCount` state na dep array do `useEffect` — `ErrorState onRetry={() => setRetryCount(c=>c+1)}`. Proibido estado de erro sem pelo menos uma ação (`onRetry`, `backTo` ou `action`) — "no dead ends". `EmptyState` exige CTA obrigatório via prop `action`. Utility `.visually-hidden` (clip/position-absolute) disponível em `index.css`.
- Responsividade mobile-first (US-034): `#root` agora é `max-width: 1280px; margin-inline: auto; padding-inline` escalando `var(--space-4)→space-5(≥640)→space-8(≥1024)` — sem `width` fixo nem `border-inline` decorativo. Tokens de breakpoint em `styles/tokens.css` (`--bp-sm/md/lg/xl`) servem de referência humana (CSS não resolve `var()` em `@media`). Globals em `index.css`: `html { -webkit-tap-highlight-color: rgba(16,185,129,0.15) }`, `button/a/[role=button]/summary/label { touch-action: manipulation }`, `input/select/textarea { font-size: 16px }` (vira `--text-sm` em ≥640px) para prevenir auto-zoom do iOS. Safe areas do notch: `.app-shell-header` aplica `padding-top/left/right: env(safe-area-inset-*)`. Hit targets ≥ 44px em mobile instalados em todos os controles interativos relevantes. Viewport meta mantém `width=device-width, initial-scale=1.0` — proibido `user-scalable=no` / `maximum-scale=1`.
- Foco em formulários migrados (US-035+): `Field` usa `useId()` apenas quando `id` não é passado — para focar programaticamente (primeiro erro de submit, autofocus condicional em desktop), **passe um `id` explícito** ao `Field` e chame `document.getElementById(id)?.focus()`. Não tente passar `ref` — a biblioteca UI ainda não expõe forwarding de ref nos wrappers. Autofocus em desktop-only usa `window.matchMedia('(min-width: 640px)')` dentro de `useEffect` (executa uma vez após mount) para evitar o teclado virtual aparecer em mobile.
- Select estilizado consistente com a biblioteca UI (sem componente próprio): `<div className="ui-field"><label htmlFor={id} className="ui-field-label">…</label><select id={id} className="ui-input" aria-describedby={…}>…</select><p className="ui-field-error|ui-field-hint">…</p></div>`. A classe `.ui-input` funciona em `<select>` porque só define borda/padding/font/min-height — nada específico de `<input>`. Use este pattern em histórias com dropdowns (US-037/041) até que um `FieldSelect` dedicado exista.
- Mapeamento de erro 409 para field-error: `extractApiError` deposita 409 em `parsed.general` (é agnóstico a domínio). Quando a história exige vincular o erro a um campo (US-036: e-mail duplicado), faça a heurística **no componente**, não no helper: `if (parsed.general && /<keyword>/i.test(parsed.general)) { nextFieldErrors[campo] = parsed.general; nextGeneral = null }`. Mantenha `extractApiError` genérico.
- Toggle mostrar/ocultar senha: botão fora do `<Field>` com `aria-pressed={mostrar}` + `aria-controls={inputId}` + label textual ("Mostrar senha"/"Ocultar senha"). `type={mostrar ? 'text' : 'password'}` é propagado pelo spread do `<Field>` ao `<Input>`. Wrapper `.signup-password` (flex column) + `.signup-password-toggle` (align-self: flex-end) — `Field` não expõe slot de adorno, então o botão vive ao lado (não dentro) do campo.
- Animação CSS compositor-friendly (US-037/US-038, reusável em US-042): barras de progresso usam `transform: scaleX(percent/100)` + `transform-origin: left center` (nunca `width`). Animações em filhos de SVG precisam de `transform-box: fill-box` + `transform-origin` declarado para que `rotate()/translateX()` trabalhem no bounding-box do próprio elemento. Staggering sem `@keyframes` duplicadas: uma única animação controlada por `--runner-speed` (custom property) + `animation-delay: calc(var(--runner-speed) * -0.3)` — alterar a variável no inline `style` (com cast `'--name' as string` para TS) reacelera tudo de uma vez. `prefers-reduced-motion` global em `index.css` neutraliza por `*`, então novas animações não precisam repetir o media query.
- Padrão "celebration/final → redirect atrasado" (US-038): separe `poll()` (só para `cancelledRef = true` quando `isFinalStatus`) do `useEffect` de redirect que observa `isCelebrating && videoId` e agenda `setTimeout(navigate, delay)` com cleanup. Reutilizável em qualquer fluxo async onde queremos dar ao usuário tempo visual antes da navegação automática. NÃO aninhe `<Link><Button/></Link>` (HTML inválido: `<a>` contendo `<button>`); use `<Button onClick={() => navigate(to)}>` com `useNavigate()`.
- Ícones e gráficos anatômicos (US-039+): inline SVG via `currentColor` + `viewBox="0 0 24 24"` + `strokeWidth` constante — colocados em `client/src/components/<dominio>/<Nome>Icon.tsx`. Para métricas biomecânicas, `client/src/components/analysis/MetricIcon.tsx` tem um switch por nome (`joelho`/`cotovelo`/`tronco`/`passada`/`oscilacao`/`simetria`/`cadencia`/`tempo`); adicionar novo tipo = novo `case` no switch + novo literal no union `MetricIconName` + campo `icon` no `METRIC_CONFIGS` correspondente. Anéis circulares de progresso (`NotaRing`) usam `strokeDasharray="<dashLen> <gap>"` + `transform="rotate(-90 cx cy)"` para começar no topo; constante `CIRCUMFERENCE = 2π·r` na escala do componente. Reutilizável para qualquer ring chart futuro.
- Paleta color-blind-friendly (US-040): `OKABE_ITO` em `client/src/utils/rawAnalysis.ts` (bluish green `#009E73` = esq, vermillion `#D55E00` = dir, blue `#0072B2`, reddish purple `#CC79A7`, etc.). Reuse-a em **qualquer** visualização futura que precise codificar lado esq/dir ou múltiplas séries — preserva contraste em deuteranopia/protanopia e em escala de cinza. Faixas ideais de métricas em `FAIXAS_IDEAIS` duplicam `server/src/biomechanics/recomendacoes.py#_classificar_*` (joelho flexão 15–25°, cotovelo 70–110°, tronco 4–8°); manter sincronizado se o backend mudar os limiares.
- Filtros por chips (US-040): para alternar visualizações, use `<div role="tablist"><button role="tab" aria-selected>` e troque `<Badge variant="success">` (ativo) por `neutral` (inativo) — consistente com a biblioteca UI, sem componente `Tab` próprio.
- Tabelas densas com paginação client-side (US-040): padrão `page` state + `deferredPage = useDeferredValue(page)`. O slice da tabela consome `deferredPage` para que o React priorize o feedback visual do clique sobre o recálculo. Mostre a posição via `<span aria-live="polite">Página X de N</span>`. Reutilizável em US-041 (histórico).
- Exports client-side (US-040): gere o texto com função pura testável (`buildCsv(points)` em `rawAnalysis.ts`), depois no componente: `new Blob([text], {type: 'text/csv;charset=utf-8'})` → `URL.createObjectURL` → âncora com `download="nome.csv"` → `click()` → `URL.revokeObjectURL`. Nunca importe bibliotecas pesadas (`file-saver`, `papaparse`).
- Sparkline (US-041, `client/src/components/historico/Sparkline.tsx`): mini-chart 100% SVG, sem `recharts`, recebe `SparklinePoint[]` (de `historicoDisplay.ts#sparklinePoints`) já ordenado cronologicamente ascendente. Escala fixa em [0,10] para a nota geral. Reuse para qualquer evolução numérica de baixa cardinalidade — preserva o bundle leve em páginas que não precisam carregar Recharts.
- Filtros via query string (US-041): cada filtro tem `parse<Filtro>Param(searchParams.get('chave'))` em `historicoDisplay.ts` e seu helper de filtragem puro (`matchesPeriodo`, `matchesStatusFilter`, `filterItems`). Convenção de URL: omitir o param quando o valor é o default (`todos`) para manter a URL canônica curta. Trocar de filtro **sempre** zera `page` (deletar `page` em vez de setar 1 mantém a URL canônica). Reusável em qualquer listagem futura com facetas.
- Restauração de scroll em master/detalhe (US-041): browser default + sessionStorage como bridge. No clique do item: `sessionStorage.setItem('historico:scroll', String(window.scrollY))` antes de `navigate(detalhe)`. No mount, dentro do `useEffect` de fetch — após `loading=false` e dados prontos — `window.scrollTo({top: saved, behavior: 'auto'})` e `removeItem`. Use `useRef(false)` para garantir restore só na primeira hidratação. Reusável em qualquer master/detalhe.
- Datas formatadas com `Intl.DateTimeFormat('pt-BR')`: instancie um formatter por concern (data + hora separados) no module-level para cachear a config; combine os outputs com espaço (`${DATE_FORMATTER.format(d)} ${TIME_FORMATTER.format(d)}`). O default da API com `dateStyle/timeStyle` adiciona vírgula entre data e hora em algumas versões do engine — separar em formatters distintos evita esse comportamento e mantém a UI estável.
- Performance e code-splitting (US-043): rotas privadas são `React.lazy()` + `<Suspense fallback={<LoadingState variant="...">}>` em `client/src/App.tsx`; Login/Signup ficam em import estático (landing sem chunk extra). Recharts vive **apenas** em `AnalysisRawPage` — a lazy-loading da página já isola ~110 KB gzipped. Se uma nova rota consumir Recharts, NÃO importe estaticamente; mantenha o componente de chart num arquivo próprio e use `lazy()`. Timeouts por-endpoint em `client/src/api/client.ts`: `DEFAULT_TIMEOUT_MS=15000` na instância + request interceptor que aplica `UPLOAD_TIMEOUT_MS=30000` (`/api/videos/upload`) e `AUTH_TIMEOUT_MS=10000` (`/api/auth/login`, `/api/users/register`). Preconnect à origem da API é injetado em runtime por `preconnectApiOrigin()` no topo de `main.tsx` — best-effort, sem depender de HTML template plugin; skip quando API é same-origin.
- Microinterações reutilizáveis (US-042): duas classes globais em `index.css` cobrem motion comum em qualquer página/feature nova — (1) `.route-fade` aplicada a um wrapper keyado por `location.pathname` em `App.tsx#RoutedApp` faz fade de página em 200ms (interruptível porque o `key` remonta o wrapper a cada navegação); (2) `.list-enter` em items de lista, com inline `style={{ ['--enter-index' as string]: idx }}` para escalonar 30ms por card via `animation-delay: calc(var(--enter-index, 0) * 30ms)`. Os botões da biblioteca UI (`.ui-button` em qualquer variante exceto `ghost`) já têm hover `translateY(-1px) + shadow-md` e active `translateY(0) + shadow-xs` em `--duration-fast` — para press affordance em qualquer novo controle, use a `.ui-button` direto (não estilize botões manualmente). Animações usam apenas `opacity`/`transform`; o reset global `prefers-reduced-motion` em `*` neutraliza tudo automaticamente — nunca repita o media query em CSS novo.

---

## [2026-04-21 08:45] - US-001 Criar schema PostgreSQL e migrations
- Added SQLAlchemy 2.0 models for `usuario`, `sessao_analise`, `metrica`, `recomendacao` with FK cascades and CHECK constraints for `status` and `severidade` enums.
- Configured Alembic under `server/alembic/` with env.py that reads `DATABASE_URL` and loads `Base.metadata` from `server.src.models`. Authored the initial revision `0001_initial_schema`.
- Installed and configured mypy (repo-root `mypy.ini`); typecheck passes over `server/src` and `server/alembic`. Migration executes cleanly against a fresh SQLite DB (verified: all four tables, unique index on `usuario.email`, FK ON DELETE CASCADE on `sessao_analise`, `metrica`, `recomendacao`).
- Files: `server/__init__.py`, `server/src/__init__.py`, `server/src/database.py`, `server/src/main.py`, `server/src/models/{__init__,usuario,sessao_analise,metrica,recomendacao}.py`, `server/alembic.ini`, `server/alembic/env.py`, `server/alembic/versions/0001_initial_schema.py`, `mypy.ini`, `requirements.txt`, `.gitignore`.
- **Learnings for future iterations:**
  - Future stories that touch the DB layer should `from server.src.database import SessionLocal` for a session factory, or add a FastAPI dependency via `get_session` (already implemented as a generator).
  - When adding new models, register them in `server/src/models/__init__.py` — otherwise Alembic won't include them in autogenerate/metadata.
  - Generating a new migration: `alembic -c server/alembic.ini revision -m "<message>"` (use `--autogenerate` once a live DB is available). Keep manual migrations dialect-portable (use `sa.false()`/`sa.func.now()` over raw literals).
  - SQLite requires `PRAGMA foreign_keys=ON` per connection to enforce FK constraints at runtime (schema is correct either way); set this in the engine for dev if FK enforcement matters in tests.
  - `server/src/main.py:read_root` has been typed (`-> dict[str, str]`) because mypy's `disallow_untyped_defs` is now strict — future endpoints must declare return types.
---

## [2026-04-21 09:30] - US-002 Endpoint de cadastro de usuário com bcrypt
- Added `POST /api/users/register` accepting `{ name, email, senha, altura_cm, peso_kg, nivel_experiencia }`, hashing the password with bcrypt and returning the public profile (no password fields).
- Validation is enforced by Pydantic (`UsuarioCreate`): `EmailStr` rejects malformed emails, `altura_cm` is required and must be in `(0, 300]`. Duplicate email returns 409 (pre-check + `IntegrityError` rollback fallback) — never 500.
- Verified all paths (200 happy/minimal, 409 duplicate, 422 missing altura/missing name/invalid email) with FastAPI `TestClient` against an in-memory SQLite (StaticPool). Mypy clean over `server/src` + `server/alembic`.
- Files: `server/src/security.py` (new), `server/src/schemas/__init__.py` (new), `server/src/schemas/usuario.py` (new), `server/src/routers/__init__.py` (new), `server/src/routers/users.py` (new), `server/src/main.py` (wired router), `requirements.txt` (added `bcrypt`, `pydantic[email]`, `httpx`).
- **Learnings for future iterations:**
  - `EmailStr` validation requires the `email-validator` package — already in `pydantic[email]` extra in `requirements.txt`. Do not import it elsewhere.
  - bcrypt 5.x ships its own backend; no need for `passlib`. The hash already encodes the salt, so no separate salt column is needed.
  - When checking for existing rows before insert, still wrap the commit in a `try/except IntegrityError` + rollback to handle the race; both branches must return the same 409 detail string.
  - The user model's `senha_hash` column is `String(255)` — bcrypt outputs ~60 chars so this is plenty, but don't shrink it.
  - Future auth dependency (US-003) should live at `server/src/security.py` alongside `verify_password` / `hash_password`, or a new `server/src/auth.py` module.
---

## [2026-04-21 10:30] - US-003 Endpoint de login e sessão autenticada
- Added `POST /api/auth/login` returning a JWT bearer token (`access_token` + `token_type`); password verified against bcrypt hash; invalid credentials and unknown email both return 401 with the same uniform `"Credenciais inválidas"` body so existence is not leaked.
- Implemented JWT helpers in `server/src/security.py` (`create_access_token` / `decode_access_token`, HS256, 60-min TTL, `SECRET_KEY` env var) and the `get_current_user` dependency in `server/src/auth.py` plus the `CurrentUser` Annotated alias for protecting future routes.
- Verified end-to-end with FastAPI `TestClient` over in-memory SQLite: happy login (200 + token), wrong password (401), unknown email (401, same body), malformed/missing fields (422), and the auth dependency (no header → 401, garbage token → 401, valid token → 200 returns the right user). Mypy clean (18 files); uvicorn boots and registers `/api/auth/login`.
- Files: `server/src/security.py` (extended), `server/src/auth.py` (new), `server/src/schemas/auth.py` (new), `server/src/routers/auth.py` (new), `server/src/main.py` (wired router), `requirements.txt` (added `pyjwt`).
- **Learnings for future iterations:**
  - Protect any private route with `def route(user: CurrentUser, ...)` — `CurrentUser` is `Annotated[Usuario, Depends(get_current_user)]` exported from `server/src/auth.py`. The dependency already injects the DB session and rejects missing/expired/invalid tokens with a 401.
  - Use `OAuth2PasswordBearer(auto_error=False)` (not `True`) so you can return your own uniform 401 body. With `auto_error=True`, FastAPI returns its own `{"detail": "Not authenticated"}` for the no-header case, which would diverge from the bad-token case and risk information leakage.
  - PyJWT 2.12+ emits `InsecureKeyLengthWarning` when the HMAC key is < 32 bytes — keep the dev default `SECRET_KEY` ≥ 32 chars to keep test/CI logs clean.
  - JWT subject is stored as `str(user_id)` in the `sub` claim; cast back to `int` in `get_current_user` and raise 401 on `ValueError` rather than 500.
  - Token TTL = 60 min (`DEFAULT_TOKEN_TTL_MINUTES`); change via the `expires_minutes` arg to `create_access_token` if a future story needs short-lived/long-lived variants.
---

## [2026-04-21 11:30] - US-004 Endpoint de upload de vídeo com validações de backend e trigger assíncrono
- Added `POST /api/videos/upload` (multipart: `file` + `pace_min_km`), auth-protected via `CurrentUser`. Validates pace 3.0–12.0, FPS ≥ 60, and lateral perspective (YOLOv8-pose shoulder/hip ratio < 0.30 median over 5 sampled frames) before creating `SessaoAnalise(status='pendente')` and dispatching `run_pipeline` as a `BackgroundTasks` job; returns `{video_id, status}`.
- Heavy deps isolated in `server/src/video_pipeline.py` behind `VideoValidator` protocol + `get_video_validator` dependency — `cv2` and `ultralytics` are lazy-imported inside methods so tests can override the validator without loading YOLO. Uploads persisted to `UPLOAD_DIR` (env var, default `./uploads/`, gitignored) and cleaned up on any validation failure.
- Mypy clean (21 source files). Smoke test (TestClient + in-memory SQLite StaticPool) covers 9 scenarios: 401 unauthenticated, 200 happy path, 422 for pace-too-fast/too-slow/missing, 422 FPS-insufficient (specific PRD message), 422 non-lateral (specific message), 422 missing file. Uvicorn boots with the new router registered.
- Files: `server/src/video_pipeline.py` (new), `server/src/schemas/video.py` (new), `server/src/routers/videos.py` (new), `server/src/main.py` (wired router), `mypy.ini` (ignore cv2/ultralytics/numpy), `requirements.txt` (added python-multipart), `.gitignore` (uploads/), `server/CLAUDE.md` (validator + upload patterns).
- **Learnings for future iterations:**
  - `BackgroundTasks` is a FastAPI-injected parameter (`background_tasks: BackgroundTasks`) — no `Depends()` wrapper. Tasks run after the response is flushed, so the HTTP client never waits on the YOLO pipeline.
  - YOLO model path comes from `YOLO_POSE_MODEL` env var (default `yolo26x-pose.pt`). The default validator returns `False` for `is_lateral` when the model weight file is missing — production must ensure `yolo26x-pose.pt` is present in CWD or `YOLO_POSE_MODEL`.
  - When raising 422/perspective errors, clean up the persisted upload first (`_cleanup(saved_path)` in `server/src/routers/videos.py`) — otherwise failed uploads accumulate in `UPLOAD_DIR`.
  - Pydantic Form-only endpoints cannot use a `BaseModel` body; each form field must be its own `Annotated[..., Form()]` parameter.
---

## [2026-04-21 12:30] - US-005 Endpoint de status do processamento
- Added `GET /api/videos/{video_id}/status` returning `{video_id, status, status_descricao}`; internal status is mapped to the PRD-facing label via `STATUS_DESCRICAO` dict in `server/src/routers/videos.py` (e.g. `detectando_pose → "Detectando pose com YOLOv8"`, `concluido → "Concluído"`). `pendente` collapses to `"Validando perspectiva"` since the background pipeline begins there.
- Ownership enforcement: `session.get(SessaoAnalise, id)` → 404 when missing, 403 when `usuario_id != user.id`. Auth reuses `CurrentUser`; missing/invalid tokens still return 401 with uniform body.
- Verified end-to-end with `TestClient` + in-memory SQLite (StaticPool): 401 no-auth, 401 garbage token, 200 + descriptive label for `pendente`/`detectando_pose`/`concluido`/`erro_multiplas_pessoas`, 403 other-user session, 404 nonexistent id. Mypy clean (21 files); uvicorn boots.
- Files: `server/src/routers/videos.py`, `server/src/schemas/video.py`.
- **Learnings for future iterations:**
  - New response schemas go in `server/src/schemas/<entity>.py` next to the existing ones (e.g. `VideoStatusResponse` lives alongside `VideoUploadResponse`). Keep them as `BaseModel` subclasses — FastAPI serialises them via `response_model=...` on the route.
  - Reuse the `STATUS_DESCRICAO` dict in future client-facing endpoints (e.g. simple/raw analysis, histórico) instead of redefining the mapping — it's the single source of truth for `sessao.status → PRD label`.
  - `session.get(Model, pk)` (SQLAlchemy 2.0) is preferred over `session.query(...).get(...)` for primary-key lookups in routers; returns `None` cleanly without raising.
---

## [2026-04-21 13:30] - US-006 Pipeline de detecção de pose YOLOv8-pose com suavização e invalidações
- Implemented real `run_pipeline(sessao_id, video_path)` in `server/src/video_pipeline.py`: drives status `pendente → detectando_pose → calculando_metricas` (or `erro_qualidade_keypoints` / `erro_multiplas_pessoas`), persists `fps` on the session, deletes the uploaded video file at end, never persists METRICA/RECOMENDACAO on error paths.
- Added `PoseExtractor` protocol + `DefaultPoseExtractor` (lazy `cv2`/`ultralytics`), `FrameKeypoints` / `PoseExtractionResult` dataclasses, `smooth_frames` (moving average, window=5, preserves None for sub-threshold keypoints), and `_predict_frame_keypoints` that filters keypoints with score `< 0.5`. Quality gate: > 50% of frames being "low quality" (≥ 9/17 invalid keypoints) → `erro_qualidade_keypoints`. Multi-person gate: > 30% of frames with `person_count > 1` → `erro_multiplas_pessoas`.
- Added `fps` column to `SessaoAnalise` + Alembic migration `0002_sessao_analise_add_fps`. Smoke-tested 5 scenarios (success / low-q / multi-person / extractor-raises / smoothing math) end-to-end against in-memory SQLite. Migration applies cleanly on a fresh DB; mypy clean (22 source files); uvicorn boots.
- Files: `server/src/video_pipeline.py`, `server/src/models/sessao_analise.py`, `server/alembic/versions/0002_sessao_analise_add_fps.py`, `server/CLAUDE.md`.
- **Learnings for future iterations:**
  - `run_pipeline` is the single in-memory chain — keypoints flow between stages as a `list[FrameKeypoints]` after `smooth_frames(...)`. Future stories (US-008, US-011, US-012, …) should extend this function in place; do **not** persist keypoints between calls.
  - Video file is deleted at the end of `run_pipeline` (success or error). Only the keypoints (in memory for that invocation) survive — design downstream stages around this.
  - The status `calculando_metricas` is the "exit" state of US-006 success; subsequent metric-calculating stories must transition the session from `calculando_metricas → concluido` once their work persists METRICA / RECOMENDACAO rows.
  - `_default_pose_extractor` and `_default_validator` are module-level singletons. To override in tests, either pass `extractor=` kwarg to `run_pipeline` or rebind the module attribute (no FastAPI override hook exists for background tasks).
  - `LOW_QUALITY_KP_THRESHOLD` is `NUM_KEYPOINTS // 2 + 1 = 9` (strict majority of 17); change `NUM_KEYPOINTS` only if the pose model changes — not if you want to tune the gate.
---

## [2026-04-21 14:30] - US-007 Utilitário de fator de escala pixels→cm
- Added `server/src/biomechanics/escala.py` with `calcular_fator_escala(frames, altura_real_cm)` returning a `FatorEscala` dataclass (`fator_escala`, `altura_pixels_media`, `frames_usados`). Altura em pixels = euclidiana entre nariz (KP 0) e ponto médio dos tornozelos (KP 15/16); frames sem os três keypoints válidos são ignorados; média entre frames válidos estabiliza o fator. `altura_real_cm` nula, zero ou negativa levanta `ValueError(MSG_ALTURA_AUSENTE)`; ausência de frames utilizáveis levanta `ValueError(MSG_SEM_FRAMES_VALIDOS)`.
- Criado pacote `server/src/biomechanics/` + `server/tests/` com 8 testes pytest (altura conhecida 175 cm / 500 px → 0.35 cm/px, média sobre múltiplos frames, ignorar frames inválidos, altura None/≤0, todos os frames inválidos, lista vazia, distância diagonal). Incluído `server/tests` em `mypy.ini`.
- Files: `server/src/biomechanics/__init__.py` (novo), `server/src/biomechanics/escala.py` (novo), `server/tests/__init__.py` (novo), `server/tests/test_escala.py` (novo), `mypy.ini` (adicionado `server/tests`). Pytest: 8 passed; mypy: 26 files clean; uvicorn boots.
- **Learnings for future iterations:**
  - Reuse `calcular_fator_escala` from US-012 (overstriding) e US-014 (oscilação vertical): ambos convertem pixels→cm via `fator_escala`. Repasse a lista suavizada (pós-`smooth_frames`) como entrada, nunca os keypoints brutos.
  - Para calcular "altura em pixels" o código usa `math.hypot(dx, dy)` (distância euclidiana), não apenas ΔY — corredor inclinado continua com altura correta. Mantenha essa convenção nas novas métricas.
  - `FrameKeypoints.keypoints[i]` pode ser `None` quando o score < 0.5; sempre faça o guard `kp is not None` antes de desempacotar `(x, y, score)`. Esta é a mesma convenção do pipeline de pose (US-006).
  - Quando o utilitário precisa de altura do usuário, obtenha via `usuario.altura_cm` (coluna `NOT NULL` no modelo). O ValueError é de segurança defensiva — em produção, o backend já garante a presença da altura no registro.
---

## [2026-04-21 11:51] - US-008 Cálculo de ângulo do joelho (esq/dir) no contato inicial
- Novo módulo `server/src/biomechanics/joelho.py` com `calcular_angulo_joelho_contato_inicial(frames)`; detecção de contato inicial por picos locais estritos de Y do tornozelo; ângulo via `arccos` do produto escalar normalizado entre `joelho→quadril` e `joelho→tornozelo` (perna estendida ⇒ 180°); média sobre os picos com keypoints válidos; retorno via `AnguloJoelhoContatoInicial(esquerdo, direito)` com `AnguloJoelhoLado(angulo_medio_graus, frames_contato)` por lado (lado sem ciclos válidos ⇒ `None`).
- `run_pipeline` agora chama `_persistir_angulos_joelho` após a transição para `calculando_metricas`, gravando `Metrica(tipo='angulo_joelho_esq'|'angulo_joelho_dir', valor=..., unidade='graus', apenas_informativa=False)` apenas para os lados com cálculo válido. Não altera o status (US-016 fará a transição final para `concluido`).
- 8 testes pytest em `server/tests/test_joelho.py` (perna estendida ⇒ 180°; flexões 90° e 120° em pontos construídos geometricamente; média sobre múltiplos ciclos; skip de frames com keypoints ausentes; sem ciclos ⇒ `None`; esq e dir independentes; lista vazia). Smoke test in-memory SQLite verifica persistência de METRICA. Mypy limpo sobre 28 arquivos; uvicorn sobe sem erros.
- Files: `server/src/biomechanics/joelho.py` (novo), `server/src/biomechanics/__init__.py` (re-export), `server/src/video_pipeline.py` (helper `_persistir_angulos_joelho`), `server/tests/test_joelho.py` (novo).
- **Learnings for future iterations:**
  - Usar `AnguloJoelhoLado(angulo_medio_graus, frames_contato)` como shape padrão para métricas por-lado com ciclos (esperado em US-010 cadência, US-012 overstriding, US-013 TCS, US-014 oscilação). Manter dataclass(frozen=True).
  - Para picos de tornozelo, o módulo usa comparação estrita (`cur > prev and cur > nxt`) e ignora bordas (índices 0 e n-1). Uma pequena diferença entre pico e vizinhos já basta. Em sintéticos, use deltas ≥ 1 px para evitar igualdade numérica.
  - Lembre-se de proteger `arccos` com `cos_theta = max(-1, min(1, cos_theta))` — ponto flutuante às vezes empurra o valor para fora de [-1,1] em configurações colineares (180°) ou dobradas (0°). Já feito em `joelho._angulo_interno` — reutilize o padrão.
  - Histórias que gravam em `METRICA` devem usar `_persistir_<metric>` isolado, com lazy-import do módulo biomecânico e do `Metrica` ORM, mantendo o corpo de `run_pipeline` legível.
---

## [2026-04-21 11:57] - US-009 Cálculo de ângulo do cotovelo (esq/dir) — média ao longo dos ciclos
- Novo módulo `server/src/biomechanics/cotovelo.py` com `calcular_angulo_cotovelo(frames)` retornando `AnguloCotoveloResultado(esquerdo, direito)`. Ângulo interno via `arccos` do produto escalar normalizado entre `cotovelo→ombro` (KP 5/6→7/8) e `cotovelo→pulso` (KP 9/10→7/8); média sobre **todos** os frames com os três keypoints válidos (não há detecção de ciclo — diferente de US-008).
- `run_pipeline` agora chama `_persistir_angulos_cotovelo` após `_persistir_angulos_joelho`, gravando `Metrica(tipo='angulo_cotovelo_esq'|'angulo_cotovelo_dir', valor=..., unidade='graus', apenas_informativa=False)` apenas quando o cálculo produz valor no lado em questão. Não altera status da sessão (US-016 fecha).
- 8 testes pytest em `server/tests/test_cotovelo.py`: 90°, 120° construído geometricamente, 180° (braço estendido), média sobre múltiplos frames, frames parcialmente válidos (ignorados), nenhum frame válido (None/None), esq e dir independentes, lista vazia. Mypy limpo (30 arquivos); pytest 24/24 passing; uvicorn sobe sem erro.
- Files: `server/src/biomechanics/cotovelo.py` (novo), `server/src/biomechanics/__init__.py` (re-export), `server/src/video_pipeline.py` (helper `_persistir_angulos_cotovelo`), `server/tests/test_cotovelo.py` (novo).
- **Learnings for future iterations:**
  - Métricas **médias sobre todos os frames** (cotovelo, tronco em apoio médio, oscilação) têm shape `AnguloXxxLado(angulo_medio_graus, frames_validos)` — `frames_validos: int`. Reserve `frames_contato: tuple[int, ...]` apenas para métricas de ciclo (joelho no contato, TCS, overstriding).
  - O helper `_angulo_interno(a, b, c)` aparece tanto em `joelho.py` quanto em `cotovelo.py` — duplicação intencional por enquanto, já que cada módulo mantém seus próprios índices COCO. Se surgir uma terceira cópia (tronco, etc.), valerá a pena extrair para `biomechanics/_angulos.py`.
  - O módulo de cotovelo **não detecta ciclos** — ele só itera frame a frame e média. Não tente forçar picos aqui: o PRD pede média simples ("sobre todos os frames válidos do vídeo").
  - Convenção mantida: braço estendido = 180°, flexão máxima → 0°. Diferente de `processing/src/mainGraph.py` (convenção `180 - internal`); backend usa interna direta.
---

## [2026-04-21 15:30] - US-010 Cálculo de cadência (spm) marcado como apenas informativo
- Novo módulo `server/src/biomechanics/cadencia.py` com `calcular_cadencia(frames, fps)` retornando `Cadencia(cadencia_spm, contatos_pe_direito, duracao_segundos)` ou `None`. Detecção de contato inicial por picos locais estritos de Y do tornozelo direito (KP 16) — mesma lógica de `joelho._find_initial_contact_indices`. Fórmula: `(n_contatos × 2 / duracao_seg) × 60`, com `duracao_seg = len(frames) / fps`. `fps <= 0` levanta `ValueError(MSG_FPS_INVALIDO)`; lista vazia ou zero picos ⇒ `None`.
- `run_pipeline` chama `_persistir_cadencia(session, sessao_id, frames, fps)` após cotovelo. Persiste uma única `Metrica(tipo='cadencia', valor=..., unidade='spm', apenas_informativa=True)` quando há valor; no-op silencioso para `fps <= 0` ou `resultado is None`. Não altera o status da sessão.
- 7 testes pytest em `server/tests/test_cadencia.py`: cadência básica (3 picos / 1s / 60fps ⇒ 360 spm), conversão via fps (mesmos frames, fps distintos ⇒ cadência proporcional), multiplicação por 2 explícita (1 pico ⇒ 120 spm), picos do pé esquerdo ignorados (pé direito ausente ⇒ None), sem picos ⇒ None, lista vazia ⇒ None, `fps <= 0` ⇒ `ValueError(MSG_FPS_INVALIDO)`. Smoke test in-memory SQLite confirma persistência em METRICA com `apenas_informativa=True`. Mypy limpo (32 arquivos); pytest 31/31 passing; uvicorn sobe sem erro.
- Files: `server/src/biomechanics/cadencia.py` (novo), `server/src/biomechanics/__init__.py` (re-export), `server/src/video_pipeline.py` (helper `_persistir_cadencia`), `server/tests/test_cadencia.py` (novo).
- **Learnings for future iterations:**
  - Métricas `apenas_informativa=True` (cadência, TCS absoluto em US-013) não afetam a nota geral mas **são** persistidas em METRICA — US-016 usa este flag para filtrar o que entra no cálculo de penalização. Ao adicionar futura métrica informativa, setar o flag explicitamente no construtor de `Metrica`.
  - Helpers de detecção de contato (`_find_initial_contact_indices` + `_ankle_y`) aparecem em `joelho.py` e agora em `cadencia.py` — duplicação intencional por hora (mesma convenção do `_angulo_interno` duplicado entre joelho/cotovelo). US-013 (TCS) provavelmente será a terceira cópia; quando houver a terceira, valerá extrair para `biomechanics/_picos.py`.
  - `_persistir_*` que dependem de dados extras do `PoseExtractionResult` (como `fps`) devem receber esse parâmetro explicitamente a partir de `run_pipeline(..., result.fps)`, mantendo a biomecânica pura (sem acesso a `sessao.fps` nem ao ORM).
  - A assinatura pública dos cálculos que precisam de tempo real segue `calcular_<metric>(frames, fps)`, levantando `ValueError(MSG_FPS_INVALIDO)` quando `fps <= 0`. Use o mesmo padrão em US-013 (TCS) que também depende de fps.
---

## [2026-04-21 16:00] - US-011 Cálculo de inclinação do tronco
- Novo módulo `server/src/biomechanics/tronco.py` com `calcular_inclinacao_tronco(frames)` retornando `InclinacaoTronco(inclinacao_media_graus, frames_validos)` ou `None`. Filtra frames em fase de apoio médio (algum joelho com flexão = 180-interno em [40°, 45°]); para cada um, calcula `topo = mean(ombros)`, `base = mean(quadris)` e a inclinação como `arctan(ΔX / -ΔY_imagem)` em graus (positivo ⇒ inclinação para frente em +X).
- `run_pipeline` chama `_persistir_inclinacao_tronco(session, sessao_id, frames)` após cadência. Persiste uma única `Metrica(tipo='inclinacao_tronco', valor=..., unidade='graus', apenas_informativa=False)` quando há valor; no-op silencioso quando nenhum frame qualifica. Não altera o status da sessão.
- 8 testes pytest em `server/tests/test_tronco.py`: 0° (ereto), 10° (frente — deslocamento tan(10°)*100 px), média sobre múltiplos frames (0° + 10° ⇒ 5°), frames fora de [40°, 45°] descartados, frame sem ombros/quadris descartado, apoio médio detectado a partir de qualquer joelho (esq ou dir), lista vazia ⇒ None, inclinação negativa para trás. Geometria do joelho de teste (quadril (100,200), joelho (100,300), tornozelo (145,350)) gera flexão = 41.99°. Mypy limpo (34 arquivos); pytest 39/39 passing; uvicorn sobe sem erro.
- Files: `server/src/biomechanics/tronco.py` (novo), `server/src/biomechanics/__init__.py` (re-export), `server/src/video_pipeline.py` (helper `_persistir_inclinacao_tronco`), `server/tests/test_tronco.py` (novo).
- **Learnings for future iterations:**
  - "Flexão" do PRD = `180 - ângulo_interno` do backend. Sempre que o PRD mencionar "joelho em flexão de X°", traduza para `interno = 180 - X` antes de comparar com o resultado de `_angulo_interno`. As constantes `FLEXAO_APOIO_MEDIO_{MIN,MAX}_GRAUS` (40, 45) ficam em `biomechanics/tronco.py` por enquanto; se US-013 (TCS) reusar a mesma faixa, considere subir para um módulo compartilhado.
  - Para qualquer métrica com sinal direcional (overstriding em US-012, simetria em US-015), preserve o sinal preservando a convenção +X = frente do corredor (para vídeos lateralmente filmados com runner movendo para a direita). Se em US-012 o sinal for problemático (ex: pé esquerdo "para trás" quando ele tem que ir +X para frente), documente a convenção próxima do call-site.
  - O helper `_angulo_interno(a, b, c)` agora aparece em três módulos (`joelho.py`, `cotovelo.py`, `tronco.py`). Conforme nota anterior em US-009, com a terceira cópia vale a pena extrair para `biomechanics/_angulos.py`. Decidi manter a duplicação por hora porque a US-011 usa o helper apenas para detecção de apoio médio (não como saída) — refatoração isolada deveria vir antes de US-012/US-013 começarem.
  - Convenção de eixo: a fórmula `arctan(ΔX/ΔY)` do PRD foi escrita assumindo Y crescendo para cima (geometria padrão); em coordenadas de imagem (Y para baixo), aplique `arctan(ΔX / -ΔY_imagem)` para que inclinação para frente saia positiva. Frames com tronco horizontal/invertido (`-ΔY ≤ 0`) são descartados como configuração inválida.
---

## [2026-04-21 17:30] - US-012 Cálculo de overstriding em cm (esq/dir)
- Novo módulo `server/src/biomechanics/overstriding.py` com `calcular_overstriding(frames, fator_escala)` retornando `OverstridingResultado(esquerdo, direito)`. Para cada frame de contato inicial (pico estrito de Y do tornozelo): `CoM_X = mean(quadril_esq_X, quadril_dir_X)`, `overstriding_px = tornozelo_X - CoM_X` (sinal preservado), conversão para cm via `× fator_escala`, média por lado sobre os ciclos com keypoints válidos.
- `run_pipeline` agora chama `_persistir_overstriding(session, sessao_id, frames, altura_cm)` após inclinação do tronco; o helper carrega `altura_cm` via `sessao.usuario`, monta o fator de escala (US-007), grava `Metrica(tipo='overstriding_esq'|'overstriding_dir', valor=..., unidade='cm', apenas_informativa=False)` apenas para os lados com cálculo válido. Falhas do fator de escala (altura ausente, nenhum frame válido) são engolidas silenciosamente — pipeline segue.
- 8 testes pytest em `server/tests/test_overstriding.py` (overstriding positivo com fator conhecido 0.5 cm/px ⇒ 25 cm; sinal negativo preservado; média sobre múltiplos ciclos; esq e dir independentes; frames sem keypoints descartados; sem picos ⇒ ambos None; lista vazia; fator de altura conhecida 175 cm/500 px ⇒ 14 cm). Smoke test in-memory SQLite verifica persistência ponta a ponta (175 cm de altura, frames sintéticos com nariz e ambos tornozelos ⇒ overstriding_esq=18.75 cm e overstriding_dir=-11.25 cm). Mypy limpo (36 arquivos); pytest 47/47 passing; uvicorn sobe sem erro.
- Files: `server/src/biomechanics/overstriding.py` (novo), `server/src/biomechanics/__init__.py` (re-export), `server/src/video_pipeline.py` (helper `_persistir_overstriding` + uso de `sessao.usuario.altura_cm` em `run_pipeline`), `server/tests/test_overstriding.py` (novo).
- **Learnings for future iterations:**
  - US-014 (oscilação vertical) reutiliza o mesmo padrão: `_persistir_oscilacao(session, sessao_id, frames, altura_cm)` carrega `altura_cm` em `run_pipeline` via `sessao.usuario.altura_cm`, calcula o fator de escala (cm/px) com `calcular_fator_escala`, e converte ΔY do CoM para cm. Não exponha `Usuario` à camada biomecânica.
  - O fator de escala usa **distância euclidiana** entre nariz e ponto médio dos tornozelos, não apenas ΔY — para corredor levemente inclinado a altura em pixels é maior que ΔY puro. Ao escrever testes com keypoints sintéticos onde há ΔX entre nariz e tornozelos, calcule o fator esperado com `math.hypot(dx, dy)` para casar com `escala.calcular_fator_escala`.
  - Sinal de overstriding é preservado deliberadamente: positivo ⇒ pé à frente do CoM (overstriding clássico), negativo ⇒ pé atrás do CoM. US-016 (recomendações) e US-018 (raw feed) consomem o sinal — não use `abs()` ao gravar a `Metrica`.
  - `sessao.usuario` é uma relação lazy do SQLAlchemy 2.0: a primeira leitura dispara um SELECT separado dentro da mesma sessão. Em `run_pipeline` isso ocorre após o `commit` que muda o status para `calculando_metricas`, ou seja, a sessão já está limpa — sem risco de "object expired" porque criamos a `SessionLocal` com `expire_on_commit=False` no `database.py` (default do projeto). Se alguma futura história acessar `.usuario` em outro ponto, não recarregue o `sessao` — o objeto está vivo durante toda a execução do pipeline.
---

## [2026-04-21 18:30] - US-013 Cálculo de Tempo de Contato com o Solo (TCS) marcado como apenas informativo
- Novo módulo `server/src/biomechanics/tcs.py` com `calcular_tcs(frames, fps)` retornando `TcsResultado(esquerdo, direito)` — cada lado como `TcsLado(tcs_medio_ms, frames_contato)` ou `None`. Detecção de ciclo com plateau aceito: início em `Y[i-1] < Y[i]` e `Y[i+1] ≤ Y[i]` (helper `_detectar_ciclos_contato`), fim na primeira queda estrita `Y[j] < Y[j-1]`. `tcs_ms = (frames_de_contato / fps) × 1000`, média sobre ciclos válidos. `fps <= 0` levanta `ValueError(MSG_FPS_INVALIDO)`.
- `run_pipeline` agora chama `_persistir_tcs(session, sessao_id, frames, fps)` após overstriding. Persiste `Metrica(tipo='tcs_esq'|'tcs_dir', valor=..., unidade='ms', apenas_informativa=True)` por lado válido. No-op silencioso para `fps <= 0` ou para lado sem ciclos detectados. Não altera status da sessão.
- 11 testes pytest em `server/tests/test_tcs.py`: detecção de ciclo com plateau (3 frames), pico estrito (1 frame), múltiplos ciclos consecutivos, conversão frames→ms via fps (50 ms @60 fps, 100 ms @30 fps), média sobre ciclos de tamanhos diferentes, esq e dir independentes, Y monotônico ⇒ None, lista vazia ⇒ ambos None, frames sem tornozelo ignorados, `None` interno quebra ciclo mas permite próximo, `fps <= 0` ⇒ `ValueError`. Smoke test in-memory SQLite confirma persistência ponta-a-ponta com `apenas_informativa=True`. Pytest 58/58 passing; mypy limpo (38 arquivos); uvicorn boots sem erro.
- Files: `server/src/biomechanics/tcs.py` (novo), `server/src/biomechanics/__init__.py` (re-export `TcsLado`, `TcsResultado`, `calcular_tcs`), `server/src/video_pipeline.py` (helper `_persistir_tcs` + chamada em `run_pipeline`), `server/tests/test_tcs.py` (novo).
- **Learnings for future iterations:**
  - A detecção de ciclo de TCS difere das outras métricas de contato: aceita plateau (`Y[i-1] < Y[i]` e `Y[i+1] ≤ Y[i]`), enquanto `_find_initial_contact_indices` exige pico estrito (`cur > prev_ and cur > nxt`). Reutilize `_detectar_ciclos_contato` de `tcs.py` apenas quando a métrica depender do **início real** do contato (plateau) e do **comprimento** do contato; para métricas "amostradas no contato inicial" (joelho, overstriding), o pico estrito ainda é correto. A refatoração cross-module prevista agora precisa contemplar ambos os modos.
  - `TcsLado(tcs_medio_ms, frames_contato)` segue o shape de métricas por-lado **com ciclos** (vs. `frames_validos: int` das métricas médias sobre todos os frames). `frames_contato` lista os `frame_idx` do início de cada ciclo (não o fim) — consistente com `AnguloJoelhoLado` e `OverstridingLado`.
  - Dois tests de "smoke test end-to-end via run_pipeline" (US-010 cadência, US-012 overstriding, agora US-013 TCS) seguem todos o mesmo padrão: `StaticPool + in-memory SQLite + Usuario + SessaoAnalise + FakeExtractor + patch('..._safe_unlink') + run_pipeline(..., delete_video=False)`. Se uma quarta história repetir esta estrutura com variações mínimas, vale extrair uma fixture pytest compartilhada em `server/tests/conftest.py`.
  - Ao adicionar métrica informativa, setar `apenas_informativa=True` **explicitamente** no `Metrica(...)` — o `server_default` de false está apenas para linhas legadas. US-016 deve filtrar pela flag em vez de listar `tipo`s específicos.
---

## [2026-04-21 19:30] - US-014 Cálculo de oscilação vertical (cm)
- Novo módulo `server/src/biomechanics/oscilacao.py` com `calcular_oscilacao_vertical(frames, fator_escala)` retornando `OscilacaoVertical(oscilacao_media_cm, ciclos_processados)` ou `None`. Cada ciclo é o intervalo fechado entre dois contatos consecutivos do pé direito (KP 16, picos estritos de Y); por ciclo, `Y_CoM = mean(Y_quadril_esq, Y_quadril_dir)`, `ΔY_pixels = max(Y_CoM) - min(Y_CoM)`; conversão para cm via `× fator_escala`. Ciclos com < 2 frames de Y_CoM válido são descartados; `< 2` contatos detectados ⇒ `None`.
- `run_pipeline` agora chama `_persistir_oscilacao_vertical(session, sessao_id, frames, altura_cm)` após overstriding; helper carrega `altura_cm` via `sessao.usuario`, monta o fator de escala (US-007) e grava `Metrica(tipo='oscilacao_vertical', valor=..., unidade='cm', apenas_informativa=False)` apenas quando há valor calculado. Falhas do fator (altura ausente/≤0, nenhum frame válido) são engolidas — pipeline segue.
- 9 testes pytest em `server/tests/test_oscilacao.py`: ΔY simples (10 cm), média sobre múltiplos ciclos, < 2 contatos ⇒ None, sem picos ⇒ None, lista vazia, ciclo sem quadris válidos descartado, fator de escala proporcional, fator realista de altura conhecida (0.35 cm/px ⇒ 7.0 cm), e smoke-test in-memory SQLite ponta-a-ponta com `Usuario(altura_cm=175)` validando persistência em METRICA. Pytest 67/67 passing; mypy limpo (40 arquivos); uvicorn sobe sem erro.
- Files: `server/src/biomechanics/oscilacao.py` (novo), `server/src/biomechanics/__init__.py` (re-export `OscilacaoVertical`, `calcular_oscilacao_vertical`), `server/src/video_pipeline.py` (helper `_persistir_oscilacao_vertical` + chamada em `run_pipeline`), `server/tests/test_oscilacao.py` (novo).
- **Learnings for future iterations:**
  - `_find_initial_contact_indices` + `_ankle_y` agora estão duplicados em **quatro** módulos (`joelho.py`, `cadencia.py`, `overstriding.py`, `oscilacao.py`). A nota das iterações anteriores previa a quarta cópia como gatilho da extração para `biomechanics/_picos.py` — neste ponto a refatoração é a próxima coisa que vale a pena fazer (mas como história separada; não a misturei com US-014). Reservar uma iteração futura.
  - Em smoke tests do pipeline com `_FakeExtractor`, a série Y do tornozelo direito influencia o **fator de escala** (porque `calcular_fator_escala` usa o midpoint `(tornozelo_esq_Y + tornozelo_dir_Y) / 2`). Para isolar o fator de escala em testes que precisam variar `tornozelo_dir_Y` (gerar picos), faça o tornozelo esquerdo "espelhar" o direito em torno de uma constante (ex: `tornozelo_esq_Y = 1400 - tornozelo_dir_Y` para midpoint = 700). Caso contrário o `altura_pixels` muda frame a frame e o fator vira a média de várias alturas — o resultado em cm fica difícil de prever de cabeça.
  - A oscilação vertical é uma **única métrica** (não tem lado esq/dir), diferente de joelho/cotovelo/overstriding/TCS. Persistida com `tipo='oscilacao_vertical'`. US-015 (simetria de oscilação) precisa olhar a série por ciclo, não a média final — o atual módulo só expõe a média; se US-015 precisar das oscilações por ciclo, será preciso estender `OscilacaoVertical` para incluir `oscilacoes_cm: tuple[float, ...]`.
  - O ângulo do helper `_y_com(frame)` retorna `None` quando algum quadril está ausente — propaga corretamente em smoothed frames sem quadris (mesma semântica de `_ankle_y`). Reutilizar o padrão para futuras métricas de CoM (ex: simetria horizontal do quadril).
---

## [2026-04-21 20:30] - US-015 Cálculo de índice de simetria (TCS, joelho, oscilação)
- Novo módulo `server/src/biomechanics/simetria.py` com `indice_simetria(esq, dir)` (pure math, `None` para lado ausente, `0.0` para degenerado `esq==dir==0`) e `calcular_simetria(frames, fps, fator_escala)` orquestrando TCS (US-013), joelho (US-008) e oscilação vertical por lado (nova função `calcular_oscilacao_vertical_por_lado` em `oscilacao.py` — ciclos do lado esq são delimitados pelos contatos do tornozelo esquerdo, do dir pelo direito). `fps <= 0 ⇒ simetria_tcs=None`; `fator_escala is None or <= 0 ⇒ simetria_oscilacao=None`.
- `run_pipeline` agora chama `_persistir_simetria(session, sessao_id, frames, fps, altura_cm)` após TCS. Persiste `Metrica(tipo='simetria_tcs'|'simetria_joelho'|'simetria_oscilacao', valor=..., unidade='%', apenas_informativa=False)` apenas para os índices calculáveis — lados ausentes ⇒ no-op por linha (critério "erro controlado" do PRD). Helper carrega `altura_cm` via `sessao.usuario`, converte em `fator_escala` via US-007, engolindo falhas do fator (altura ausente ou nenhum frame válido) ⇒ simetria de oscilação ⇒ None sem interromper o pipeline.
- 13 testes pytest em `server/tests/test_simetria.py`: fórmula padrão, lados iguais ⇒ 0%, invariância de ordem, lado ausente ⇒ None (3 casos), ambos zero ⇒ 0.0, lados iguais ⇒ 0 em TCS+joelho+oscilação, joelho assimétrico (180° vs 90° ⇒ ~66.67%), joelho com lado ausente, TCS dois lados, FPS inválido ⇒ só degrada TCS, oscilação dois lados, fator None ⇒ só degrada oscilação, lista vazia ⇒ tudo None. Pytest 80/80 passing; mypy limpo (42 arquivos); uvicorn boots.
- Files: `server/src/biomechanics/simetria.py` (novo), `server/src/biomechanics/oscilacao.py` (adicionou `OscilacaoVerticalPorLado` + `calcular_oscilacao_vertical_por_lado`, refatorou `calcular_oscilacao_vertical` em `_calcular_oscilacao_para_tornozelo` private helper), `server/src/biomechanics/__init__.py` (re-exports), `server/src/video_pipeline.py` (helper `_persistir_simetria` + chamada em `run_pipeline`), `server/tests/test_simetria.py` (novo).
- **Learnings for future iterations:**
  - `indice_simetria(esq, dir) -> float | None` é agora o único ponto de cálculo de assimetria do projeto. US-016 (nota geral) pode consumir as `Metrica(tipo='simetria_*', unidade='%')` diretamente do DB, ou chamar `calcular_simetria` de novo sobre os frames — mas a convenção é não recalcular: as simetrias já estão persistidas em METRICA pelo pipeline.
  - Para oscilação vertical, a versão "por lado" (`calcular_oscilacao_vertical_por_lado`) delimita ciclos pelos contatos de **cada** pé separadamente. A função legada `calcular_oscilacao_vertical` (usada pela US-014) continua existindo e usa só o pé direito — mantém compatibilidade com a métrica média única `tipo='oscilacao_vertical'`.
  - Degradação graceful da camada orquestradora: quando o cálculo "dependencial" (fator de escala, fps) falha, a simetria correspondente vira `None` sem levantar exceção — o pipeline segue e persiste apenas o que é calculável. US-016 deve adotar o mesmo padrão (recomendações geradas apenas para métricas presentes).
  - Caso degenerado `esq == dir == 0` ⇒ `indice_simetria` retorna `0.0` em vez de levantar (divisão por zero). Fisicamente ambos os lados zerados significa "perfeitamente simétricos" — não é erro controlado de "lado ausente" (esse caso é coberto por `None` em qualquer operando).
---

## [2026-04-21 21:30] - US-016 Gerador de recomendações + nota geral com tabela de thresholds
- Novo módulo `server/src/biomechanics/recomendacoes.py` com `analisar_metricas(metricas) -> AnaliseResultado` (nota_geral ∈ [0,10], feedback_ia, tupla de `RecomendacaoGerada`). Tabela da Seção 9 do PRD implementada por métrica: joelho (traduz interno→flexão), cotovelo, inclinação tronco, overstriding (classifica por `abs(valor)`, preserva sinal no texto), oscilação vertical e simetria (TCS, joelho, oscilação — mesma tabela para os três). Dispatch via dict `_ANALISADORES` (um analisador por `tipo` de `Metrica`); tipos desconhecidos são silenciosamente ignorados. Nota = 10 − Σpesos, clamped em [0,10]. Pesos: `PESO_ATENCAO=0.5`, `PESO_CRITICO=1.5` (crítico intencionalmente 3× atenção); informativo não penaliza.
- `run_pipeline` agora chama `_finalizar_analise(session, sessao_id)` após `_persistir_simetria`. O helper carrega todas as `Metrica` da sessão, filtra `apenas_informativa=True` (cadência, TCS absoluto — conforme PRD FR-16), gera `Recomendacao` (categoria, descrição acionável, severidade) para cada métrica conhecida, persiste `nota_geral`/`feedback_ia` em `SESSAO_ANALISE` e transita status para `concluido`. Esta é a **única** transição para `concluido` do pipeline — demais `_persistir_*` não mexem no status.
- 28 testes pytest em `server/tests/test_recomendacoes.py`: classificação por faixa para cada métrica (ideal/atenção/crítico, com magnitudes positivas e negativas para overstriding e tronco), clamp da nota em 0 (11 métricas críticas ⇒ penalização 16.5), crítico > atenção em peso, tipos desconhecidos ignorados, feedback textual com "Nota geral", lista vazia ⇒ nota 10. Smoke tests end-to-end: (a) `run_pipeline` com frames sintéticos completos grava recomendações, nota e status `concluido`; (b) `_finalizar_analise` isolado com seed direto do DB confirma que `apenas_informativa=True` (cadência, TCS absoluto) não penaliza a nota nem gera recomendação. Pytest 108/108 passing; mypy limpo (44 arquivos); uvicorn sobe sem erro.
- Files: `server/src/biomechanics/recomendacoes.py` (novo), `server/src/biomechanics/__init__.py` (re-exports), `server/src/video_pipeline.py` (helper `_finalizar_analise` + chamada em `run_pipeline`), `server/tests/test_recomendacoes.py` (novo).
- **Learnings for future iterations:**
  - `_finalizar_analise` concentra **toda** a lógica de fechamento da sessão: recomendações + nota + feedback + status. Endpoints da próxima onda (US-017 diagnóstico simples, US-019 histórico, US-025 cards por severidade) devem ler `SessaoAnalise.nota_geral`/`feedback_ia` e `Recomendacao.*` sem recalcular — o pipeline já materializou tudo. Se uma história futura precisar reclassificar uma métrica isolada, reutilize `analisar_metricas([(tipo, valor)])` em vez de rodar o pipeline inteiro.
  - Textos de recomendação são em português com o valor medido formatado no texto (`f"{valor:.1f}°"`). US-025 (frontend simplificado) pode renderizar `categoria` como título do card e `descricao` como corpo — não há jargão técnico sem explicação. Se a UI precisar do valor numérico separado, a `Metrica` correspondente já está na tabela (`Metrica.valor`/`unidade`); a `Recomendacao` é prose-only.
  - A fórmula de convenção do joelho "flexão = 180 − interno" vive em dois lugares: `biomechanics/tronco.py` (para detecção de apoio médio) e agora `biomechanics/recomendacoes.py` (para threshold da Seção 9). Se uma terceira história precisar dessa conversão, extraia para `biomechanics/_angulos.py` (junto com o helper duplicado `_angulo_interno` que já motivou nota em US-011).
  - Filtramos `apenas_informativa` em Python (não em SQL `WHERE col == False`) para evitar `comparação-a-True/False` que o mypy/lint costumam reclamar e para preservar portabilidade entre SQLite/PG. Com ≤ 11 métricas por sessão, o custo é irrelevante.
  - Quando um teste reatribui uma variável `sessao` depois de um `session.get(...)` que retorna `Optional[Model]`, mypy (disallow_untyped_defs) marca assignment mismatch contra a primeira atribuição (construção direta). Use um nome diferente (ex: `sessao_final`) ou faça `assert` depois de cada `get`. Padrão já aplicado em outros smoke tests (US-012/US-014).
---

## [2026-04-21 22:30] - US-017 Endpoint de diagnóstico simplificado
- Novo router `server/src/routers/analysis.py` com `GET /api/analysis/{analysis_id}/simple`; protegido por `CurrentUser`, retorna `AnalysisSimpleResponse { nota_geral, feedback_ia, recomendacoes, metricas_resumidas, erro }`. Ownership checks: 404 quando `session.get(SessaoAnalise, id) is None`, 403 quando `sessao.usuario_id != user.id`.
- Para status de erro (`erro_qualidade_keypoints`, `erro_multiplas_pessoas`), responde 200 com `erro` populado pela mensagem de `STATUS_DESCRICAO` (importada de `routers/videos.py` como fonte única de rótulos) e `recomendacoes`/`metricas_resumidas` como listas vazias — `nota_geral` e `feedback_ia` permanecem `None`.
- Novo `server/src/schemas/analysis.py` com `RecomendacaoResponse`, `MetricaResumida` e `AnalysisSimpleResponse` (Pydantic v2, `ConfigDict(from_attributes=True)` para conversão direta de ORM rows via `model_validate`). Router registrado em `server/src/main.py` via `app.include_router(analysis.router)`.
- Verificado end-to-end com `TestClient` + in-memory SQLite (StaticPool) em 8 cenários: 401 sem auth, 401 token inválido, 200 feliz com sessão concluída (valores + 2 recomendações + 2 métricas), 200 com `erro_qualidade_keypoints` (campo `erro` = "Erro: qualidade de keypoints insuficiente"), 200 com `erro_multiplas_pessoas`, 403 acessando sessão alheia, 404 id inexistente, 200 para segundo usuário na sua própria sessão. Mypy limpo (46 arquivos); pytest 108/108 passing; uvicorn sobe sem erro.
- Files: `server/src/routers/analysis.py` (novo), `server/src/schemas/analysis.py` (novo), `server/src/main.py` (wired router).
- **Learnings for future iterations:**
  - US-018 (raw feed) e US-019 (histórico) seguem o mesmo shape: `GET /api/analysis/{id}/raw` pode reusar o `analysis_id` path param, `CurrentUser` dep, e os checks 404/403 deste router. Se virarem múltiplos endpoints no mesmo prefix `/api/analysis`, manter tudo em `routers/analysis.py` e expandir conforme a necessidade.
  - `STATUS_DESCRICAO` (definido em `routers/videos.py`) é reutilizável por qualquer endpoint client-facing que precise traduzir `sessao.status` para texto. Não reimplementar o dict em novos routers.
  - Para schemas de resposta que espelham ORM rows (Recomendacao → RecomendacaoResponse, Metrica → MetricaResumida), `ConfigDict(from_attributes=True)` elimina a necessidade de mapeamento manual. `Model.model_validate(orm_row)` faz o trabalho.
  - Endpoints de análise devem sobreviver aos três estados possíveis da sessão: (a) ainda processando (sem nota/feedback; listas vazias ou parciais), (b) concluído (tudo populado), (c) erro de pipeline (retornar `erro` + listas vazias). Hoje o router trata (a) igual a (b) — `nota_geral`/`feedback_ia` vêm `None`; se US-024 forçar o cliente a só chamar `/simple` pós-concluído, nenhuma mudança aqui é necessária.
---

## [2026-04-21 23:30] - US-018 Endpoint de dados biomecânicos brutos
- Novo `GET /api/analysis/{id}/raw` em `server/src/routers/analysis.py` que retorna `{ fps, frames: [...], metricas_agregadas: [...], simetria: { tcs, joelho, oscilacao }, erro }`. Ownership idêntico ao `/simple` (401/403/404); status de erro (`erro_qualidade_keypoints`, `erro_multiplas_pessoas`) respondem 200 com `erro` populado e listas vazias. Métricas `simetria_*` são drenadas para o sub-objeto `simetria` e **não** aparecem em `metricas_agregadas`.
- Novo módulo `server/src/biomechanics/frame_metrics.py` com helpers per-frame (`angulo_joelho_frame`, `angulo_cotovelo_frame`, `inclinacao_tronco_frame`, `y_com_frame`) reutilizando constantes de índices COCO dos módulos agregados. Adicionada coluna `SessaoAnalise.dados_brutos_json` (TEXT) + migration `0003_sessao_analise_add_dados_brutos`. O pipeline (`_persistir_dados_brutos`) serializa keypoints (17×`[x,y,score]` ou `null`) + per-frame metrics para JSON puro (`allow_nan=False`) logo após entrar em `calculando_metricas`.
- Novos schemas `AnalysisRawResponse`, `RawFrame`, `SimetriaResponse` em `server/src/schemas/analysis.py`. 16 testes novos em `server/tests/test_frame_metrics.py` + 10 testes em `server/tests/test_analysis_raw.py` (TestClient + in-memory SQLite StaticPool cobrindo 401/403/404/feliz/erro_qualidade/erro_multiplas/dados_brutos_null/pipeline_ponta_a_ponta/sem NaN). Pytest 133/133 passing; mypy limpo (50 arquivos); migration 0003 aplica em DB limpo; uvicorn boots com 11 rotas incluindo `/api/analysis/{id}/raw`.
- Files: `server/src/models/sessao_analise.py` (+ coluna `dados_brutos_json`), `server/alembic/versions/0003_sessao_analise_add_dados_brutos.py` (nova), `server/src/biomechanics/frame_metrics.py` (novo), `server/src/biomechanics/__init__.py` (re-exports), `server/src/video_pipeline.py` (`_persistir_dados_brutos` + chamada em `run_pipeline`), `server/src/schemas/analysis.py` (+ `AnalysisRawResponse`/`RawFrame`/`SimetriaResponse`), `server/src/routers/analysis.py` (+ `GET /raw`), `server/tests/test_frame_metrics.py` (novo), `server/tests/test_analysis_raw.py` (novo).
- **Learnings for future iterations:**
  - Persistência de dados pesados por sessão (keypoints × frames) via coluna `TEXT` nullable com JSON serializado é simples e portátil entre SQLite e Postgres. Para vídeos longos (3 min @60 fps = ~10k frames × 17 keypoints) espere ~10–30 MB por linha — se virar problema, mover para tabela dedicada `QuadroAnalise(sessao_id, frame_idx, keypoints_json, ...)` ou blob gzipado. Por enquanto JSON inline é OK para o MVP.
  - `json.dumps(..., allow_nan=False)` é o guard correto para satisfazer "nunca NaN na serialização JSON": levanta `ValueError` se algum valor `float('nan')` escapar, falhando o pipeline em vez de gerar payload inválido. Use `None` (→ `null`) em todo o helper per-frame para keypoints/ângulos ausentes.
  - US-019 (histórico) vai reusar a mesma convenção de schema: `ConfigDict(from_attributes=True)` para listas de `SessaoAnalise` rows, controle de acesso por `user.id == sessao.usuario_id`, limites clamp (`limit` máx 50 ⇒ PRD). Ainda não foi implementado — estrutura do router `analysis.py` já suporta expansão.
  - Para testes que usam TestClient com autenticação, `create_access_token(user_id)` (em `server/src/security.py`) gera o JWT diretamente — não precisa passar por `/api/auth/login`. O header fica `Authorization: Bearer <token>` e já é reconhecido pela dependência `CurrentUser`.
---

## [2026-04-21 23:45] - US-019 Endpoint de histórico paginado
- Novo router `server/src/routers/historico.py` com `GET /api/historico-analise?page=1&limit=10`. `page`/`limit` validados como `Query(ge=1)` (zero/negativo ⇒ 422, não-numérico ⇒ 422); `limit > 50` é clamped silenciosamente para `LIMIT_MAXIMO = 50`. Query SQLAlchemy filtra `usuario_id == user.id`, ordena `criado_em DESC`, e faz `offset((page-1)*limit).limit(limit)`; `total` calculado com `select(func.count(SessaoAnalise.id))`. Retorna `HistoricoResponse { items: [{ id, criado_em, pace_min_km, status, nota_geral }], total, page, limit }`.
- Novos schemas em `server/src/schemas/historico.py` (`HistoricoItem` com `ConfigDict(from_attributes=True)` para conversão ORM-direta; `HistoricoResponse` agregador). Router registrado em `server/src/main.py` ao lado dos demais.
- 14 testes pytest em `server/tests/test_historico.py` cobrindo: 401 sem auth, 401 token inválido, usuário sem sessões ⇒ lista vazia, isolamento entre usuários, ordenação `criado_em DESC`, shape completo do item, paginação em 3 páginas (limit=2 sobre 5 sessões), clamp de `limit=100` para 50 em 60 sessões, 422 para `page=0`, 422 para `page=-1`, 422 para `limit=0`, 422 para `limit=abc`, defaults `page=1&limit=10`, sessões com status de erro continuam no histórico com `nota_geral=None`. Pytest 147/147 passing; mypy limpo (53 arquivos); uvicorn boots sem erro.
- Files: `server/src/routers/historico.py` (novo), `server/src/schemas/historico.py` (novo), `server/src/main.py` (wired router), `server/tests/test_historico.py` (novo).
- **Learnings for future iterations:**
  - `criado_em` da `SessaoAnalise` tem `server_default=func.now()`, então testes que dependem de ordenação determinística precisam setar `criado_em=datetime(...)` explicitamente na construção do ORM — não confie na ordem de inserção (duas linhas criadas no mesmo segundo quebram o test). O helper `_seed_sessoes(factory, user_id, count=N)` em `test_historico.py` escalona `timedelta(minutes=i)` e é reutilizável em futuros tests que precisem de N sessões ordenadas.
  - Clamp vs. reject: quando o PRD diz "valores acima são clamped; valores inválidos retornam 422", resolva usando `Query(ge=1)` (ge sem le) + `min(limit, LIMIT_MAXIMO)` no corpo — FastAPI já gera 422 para valores ≤ 0 ou não-numéricos, e o clamp silencioso acontece em Python. Não adicione `le=50` ao `Query` (isso tornaria `limit=100` ⇒ 422, violando o critério).
  - `session.scalar(select(func.count(Model.id)).where(...))` é a forma SQLAlchemy 2.0 para contagens. Fazer cast `int(total or 0)` defensivo para satisfazer mypy (retorno é `Any | None`).
  - Convenção de prefixo: quando o endpoint PRD é standalone (não cabe em `/api/<recurso>/{id}/...`), crie um router dedicado com `APIRouter(prefix="/api", ...)` e `@router.get("/historico-analise", ...)`. Evita poluir `routers/analysis.py` com shapes de paginação que não pertencem ali.
  - Pydantic v2 serializa `datetime` com timezone para ISO-8601 com offset (`"2026-04-01T12:00:00+00:00"`); se o frontend precisar de formato específico (ex: Date em JS `new Date()`), ISO-8601 já funciona. Sem serializer custom necessário.
---

## [2026-04-21 13:35] - US-020 Scaffold do frontend React (Vite + rotas + cliente HTTP + contexto de auth)
- Inicializado projeto Vite + React 19 + TS 6 em `client/` (scaffolding `create-vite --template react-ts`) + `npm install react-router-dom axios`. Removidos assets/CSS de demo; `src/index.css` mantido (typography reset benigno).
- Cliente HTTP (`src/api/client.ts`): axios com `API_BASE_URL = import.meta.env.VITE_API_BASE_URL ?? "http://localhost:8000"` e interceptor request que injeta `Authorization: Bearer <token>` via `localStorage[TOKEN_STORAGE_KEY]`. Chamadas tipadas em `src/api/auth.ts` (`loginRequest`, `signupRequest`, tipos de payload/response).
- Auth context (`src/context/AuthContext.tsx`): `AuthProvider` + hook `useAuth()` expõe `{ user, token, isAuthenticated, login, signup, logout }`. Token persistido em localStorage; `user.id` extraído do `sub` do JWT via decodificação manual (sem dep externa). `signup` faz register + login automático. `PrivateRoute` em `src/components/PrivateRoute.tsx` redireciona a `/login` preservando `state.from`. Rotas em `src/App.tsx` cobrem `/login`, `/signup`, `/upload`, `/status/:id`, `/analysis/:id`, `/analysis/:id/raw`, `/historico` (placeholders em `src/pages/`); fallback `*` → `/login`. Typecheck via `npm run build` passa (tsc -b + vite build 273.65 kB gzipped 89.60 kB); `npm run dev` serve HTTP 200 em :5173.
- Files: `client/` scaffold completo (Vite config, tsconfigs, index.html, public/*), `client/package.json`, `client/package-lock.json`, `client/.env.example`, `client/src/main.tsx` (do scaffold), `client/src/App.tsx` (reescrito), `client/src/index.css` (mantido), `client/src/api/{client,auth}.ts`, `client/src/context/AuthContext.tsx`, `client/src/components/PrivateRoute.tsx`, `client/src/pages/{Login,Signup,Upload,Status,Analysis,AnalysisRaw,Historico}Page.tsx`, `client/CLAUDE.md` (novo).
- **Learnings for future iterations:**
  - US-021 (telas de login/cadastro) pode consumir `useAuth().login(...)` e `useAuth().signup(...)` diretamente — as funções já lidam com persistência do token e atualização do estado. Use `useNavigate()` + `useLocation()` para ler `state.from` e voltar à rota de origem após login.
  - O tipo `UserProfile` em `src/api/auth.ts` reflete o shape de `POST /api/users/register` (id, name, email, altura_cm, peso_kg, nivel_experiencia). Quando o backend expuser `GET /api/users/me`, basta adicionar a função + chamar no `AuthProvider` para hidratar `user` com dados completos em login (hoje só temos `{ id }` após login porque o endpoint retorna só o token).
  - Dev-browser skill não está disponível neste ambiente — a verificação em browser da história foi substituída por `curl http://localhost:5173/` (HTTP 200) + inspeção do bundle HTML gerado pelo Vite dev server. Para histórias futuras que exigirem dev-browser, ou o skill é instalado ou esse workaround continua aceitável.
  - Erros de API no frontend: o axios lança quando a resposta não é 2xx. `useAuth().login/signup` propagam o erro — as telas (US-021) devem `try/catch` e extrair `error.response.data.detail` para exibir (409 e-mail duplicado, 401 credenciais inválidas, 422 campos inválidos com detalhe de campo).
---

## [2026-04-21 20:55] - US-021 Telas de cadastro e login
- Implementada `LoginPage` (`/login`): campos e-mail/senha, validação client-side obrigatória, `useAuth().login()` + redirect para `state.from` (fallback `/upload`); se já autenticado, `<Navigate>` para `redirectTo`.
- Implementada `SignupPage` (`/signup`): campos nome, e-mail, senha (≥6), altura_cm (obrigatório, 1–300), peso_kg (opcional), nivel_experiencia (select); usa `useAuth().signup()` que faz register + login automático e redireciona para `/upload`. Erros de API exibidos via helper compartilhado `extractApiError(err)` (geral + por-campo).
- Criado `client/src/api/errors.ts` (helper axios → `{general, fields}`), estilos `auth-*` adicionados a `client/src/index.css`. `npm run build` (tsc + vite) 0 erros. `npm run dev` sobe 200 em :5173 (dev-browser skill indisponível no ambiente — verificação visual não foi realizada).
- Files: `client/src/api/errors.ts` (novo), `client/src/pages/LoginPage.tsx`, `client/src/pages/SignupPage.tsx`, `client/src/index.css`.
- **Learnings for future iterations:**
  - `extractApiError(err)` cobre as quatro situações reais de erro (401, 409, 422 com lista de campos, e falha de rede). Reutilize em US-023 (upload com 422 de FPS/perspectiva), US-024 (status com 403/404), US-025/US-026 (análise), US-027 (histórico).
  - Guard "já autenticado" nas telas de auth: `if (isAuthenticated) return <Navigate to={...} replace />` no próprio corpo do componente — mesma convenção do `PrivateRoute`. Não use `useEffect` + `navigate()` (causa flash de conteúdo).
  - Redirect pós-login lê `location.state.from` (tipado como `RedirectState`) — `PrivateRoute` já populou isso. Sempre use `replace: true` nesse navigate para não empilhar a página de login no histórico do browser.
  - `noValidate` no `<form>` desabilita validação nativa do browser para que a validação React dite a UX de mensagens (evita tooltips nativos competindo com `.field-error`).
  - `erasableSyntaxOnly: true` do tsconfig impede `enum` — use `as const` array of `{value, label}` (padrão em `NIVEIS_EXPERIENCIA` na SignupPage) para dropdowns tipados.
---

## [2026-04-21 21:00] - US-022 Componente de input de pace com conversão pace↔km/h em tempo real
- Criado `client/src/utils/pace.ts` com helpers puros (`paceTextToDecimal`, `paceDecimalToKmH`, `kmHToPaceParts`, `formatPaceParts`, `formatKmH`, `isPaceInRange`, `isKmHInRange`) seguindo as fórmulas do PRD: `km_h = 60 / pace_decimal`, `pace_min = floor(60 / km_h)`, `pace_seg = round((60/km_h % 1) × 60)`. Carry-over de 60s → próximo minuto coberto.
- Criado componente `client/src/components/PaceInput.tsx` com dois inputs vinculados (Pace `min:ss` + Velocidade `km/h`). Editar um atualiza o outro em tempo real; valida faixa 3:00–12:00 min/km (5–20 km/h); expõe estado `{paceMinKm, isValid, errorMessage}` via callback `onChange` para o pai (consumido em US-023). `UploadPage` integra o componente como esqueleto da tela.
- Instalado Vitest (`npm install -D vitest`) + script `npm test`. 13 testes em `src/utils/pace.test.ts` cobrem parsing, conversão bidirecional, roundtrip 5:30 ↔ 10.9 km/h, carry-over de segundos, formatação e guards de faixa. `npm test` (13/13 passing), `npm run build` (tsc + vite, 0 erros), `npm run dev` sobe HTTP 200 em `/upload`.
- Files: `client/src/utils/pace.ts` (novo), `client/src/utils/pace.test.ts` (novo), `client/src/components/PaceInput.tsx` (novo), `client/src/pages/UploadPage.tsx` (integração), `client/src/index.css` (classes `.pace-input*`), `client/package.json` (script `test` + dep `vitest`), `client/CLAUDE.md` (notas sobre `npm test` e convenção `src/utils/`).
- **Learnings for future iterations:**
  - `PaceInput` é o componente canônico de pace — qualquer tela futura (US-023 upload, recadastro de pace de uma sessão) deve consumi-lo via `<PaceInput onChange={setPace} />` e ler `pace.paceMinKm` (decimal min/km) para enviar ao backend. O backend já valida a faixa de novo (US-004), mas o frontend bloqueia o envio antes via `pace.isValid`.
  - Conversão pace ↔ km/h: para apresentação, `formatKmH(kmH)` arredonda para 1 casa decimal — é o que faz o roundtrip 5:30 ↔ "10.9" funcionar (10.9 → 5:30 via `kmHToPaceParts`). Não use precisão maior nessa exibição ou o usuário verá variações pequenas ao alternar inputs.
  - Vitest roda em ambiente Node por padrão (sem JSDOM); para testes de componente React em histórias futuras, adicionar `jsdom` + `@testing-library/react` + `test.environment: 'jsdom'` na `vite.config.ts`. Por enquanto só temos testes de utilitários puros.
  - O componente `PaceInput` envolve os dois inputs em `<fieldset>` + `<legend>` (não em `.field` direto) — semântica correta para um par de inputs vinculados. Estilos `.pace-input*` em `index.css`.
---

## [2026-04-21 21:10] - US-023 Tela de upload com validações de frontend + integração com API
- Implementada `UploadPage` (`/upload`) integrando `PaceInput` (US-022) + input de arquivo `<input type="file" accept="video/*">`. Lê metadados via `readVideoMetadata(file)` (HTMLVideoElement em memória + `URL.createObjectURL`); estimativa de FPS best-effort via `requestVideoFrameCallback` (null quando indisponível). Validação pura `validateVideoMetadata(meta)` bloqueia duração <30s/>3min, resolução <640×480, e emite warning amarelo para FPS <60. Botão habilitado só com vídeo válido + pace válido. POST `/api/videos/upload` (form-data) → redirect `/status/:video_id`; erros 422 (FPS/perspectiva) exibidos via `extractApiError`.
- Files: `client/src/api/videos.ts` (novo, helper `uploadVideoRequest`), `client/src/utils/videoValidation.ts` (novo, pura), `client/src/utils/videoValidation.test.ts` (novo, 11 testes), `client/src/utils/videoMeta.ts` (novo, DOM-dependent), `client/src/pages/UploadPage.tsx` (reescrita), `client/src/index.css` (classes `.upload-info`, `.upload-meta-summary`, `.upload-warning`), `client/CLAUDE.md` (notas sobre validação de vídeo + contrato com `/api/videos/upload`).
- `npm test` 24/24 passing (13 pace + 11 vídeo). `npm run build` (tsc + vite, 0 erros, 287 KB gzipped 93 KB). `npm run dev` sobe HTTP 200 em `/upload`. Dev-browser skill indisponível no ambiente — verificação visual substituída por `curl http://localhost:5173/upload` (HTTP 200) + inspeção do build estático.
- **Learnings for future iterations:**
  - `lib.dom.d.ts` do TypeScript 6 já expõe `requestVideoFrameCallback`, `VideoFrameRequestCallback` e `VideoFrameCallbackMetadata` — não redeclare locamente (causa conflito `incorrectly extends`). Use direto `VideoFrameRequestCallback` como tipo do callback.
  - Validação local de vídeo é split em duas camadas: `videoValidation.ts` (puro, testável em Node sem JSDOM) e `videoMeta.ts` (DOM-dependent). Pure tests cobrem todos os ramos de erro/warning; o reader DOM é validado manualmente no browser. Padrão reutilizável para US-026 se precisar inspecionar metadados de qualquer outro arquivo.
  - FormData multipart com axios: nunca defina `Content-Type` manualmente — o browser/axios anexa automaticamente o boundary. O interceptor de auth do projeto continua funcionando (apenas adiciona `Authorization`, não toca em `Content-Type`).
  - Para inputs file controlados por React, use `event.target.files?.[0] ?? null` — `null` é o estado canônico de "sem arquivo" e cobre o caso do usuário cancelar o picker (alguns browsers retornam `FileList` vazio em vez de `null`).
  - Padrão de redirect pós-ação bem-sucedida: `useNavigate()` + `navigate(\`/status/\${id}\`)` — não use `<Navigate>` aqui (handler é assíncrono, precisa imperativo). Para flows de auth/guard, manter `<Navigate>` no corpo do componente.
---

## [2026-04-21 21:15] - US-024 Tela de status do processamento com polling
- Novo `client/src/pages/StatusPage.tsx` com polling em `GET /api/videos/{id}/status` a cada 2s via `setTimeout` auto-reagendado em `useEffect`. Estados renderizados: barra de progresso (0–100%), lista ordenada de 4 etapas (Validando perspectiva → Detectando pose com YOLOv8 → Calculando métricas → Concluído) com marcador `done`/`active`, descrição corrente. `concluido` → `navigate(/analysis/:id, {replace:true})`. Estados de erro (`erro_qualidade_keypoints`, `erro_multiplas_pessoas`) renderizam mensagem do PRD + botão "Enviar novo vídeo" que volta para `/upload`. Polling cancelado no cleanup via `cancelledRef` + `clearTimeout`; falhas transitórias de rede exibem erro mas continuam o polling.
- Novo `client/src/utils/videoStatus.ts` (puro) + `videoStatus.test.ts` (Vitest, 11 testes): mapeamento status→estágio, `progressPercentFromStatus`, `isErrorStatus`, `errorMessageForStatus`, `isFinalStatus`. Constantes `STATUS_*` e mensagens de erro reusáveis em US-025/US-027. `client/src/api/videos.ts` ganhou `getVideoStatusRequest(videoId)` + interface `VideoStatusResponse` alinhada ao contrato do backend. CSS novo em `index.css` (`.status-progress`, `.status-stage-list`, `.status-stage-done/active`, `.status-restart-button`).
- `npm test` 35/35 passing (24 antigos + 11 novos). `npm run build` (tsc + vite) limpo, 290 KB gzipped 94 KB. `npm run dev` sobe sem erro em `http://localhost:5173`. Dev-browser skill indisponível no ambiente — verificação substituída por build + testes unitários puros cobrindo a lógica de mapeamento.
- Files: `client/src/pages/StatusPage.tsx` (reescrito), `client/src/api/videos.ts` (getVideoStatusRequest), `client/src/utils/videoStatus.ts` (novo), `client/src/utils/videoStatus.test.ts` (novo), `client/src/index.css` (estilos status-*).
- **Learnings for future iterations:**
  - Polling com `setTimeout` auto-reagendado (cada resposta agenda a próxima) é preferível a `setInterval` quando o backend pode demorar variando: evita acúmulo de requests pendentes + permite parar imediatamente em estados finais (`concluido`/`erro_*`). Sempre use `useRef<number | null>(null)` para o timer id e um `cancelledRef` separado para descartar respostas chegadas pós-unmount.
  - Padrão para componente com cleanup assíncrono: dentro do `useEffect` declare `cancelledRef.current = false` no início, marque `true` na cleanup, e nos then/catch faça `if (cancelledRef.current) return` antes de `setState`. Evita React warning "state update on unmounted component".
  - Redirect pós-sucesso dentro de efeito: use `navigate(path, { replace: true })` para não poluir o histórico com a tela de status (o usuário não deve conseguir voltar para ela após a análise concluir). Alinha com o mesmo padrão usado em `PrivateRoute` (`<Navigate replace />`).
---

## [2026-04-21 21:25] - US-025 Tela de diagnóstico simplificado com cards por severidade
- Reescrita `AnalysisPage` (`/analysis/:id`): consome `GET /api/analysis/{id}/simple` via novo helper `getSimpleAnalysisRequest` em `src/api/analysis.ts`. Renderiza card destaque da nota (cor por faixa: `nota-good ≥7`, `nota-ok ≥4`, `nota-bad <4`), parágrafo de feedback IA, gauges horizontais por métrica (barra com zona ideal realçada em verde + marcador roxo da posição medida + rótulos de faixa), e cards de recomendação agrupados por severidade em 3 seções (`Pontos críticos` vermelho, `Pontos de atenção` amarelo, `Já está dentro do ideal` verde). Link final "Ver dados técnicos →" navega para `/analysis/:id/raw`. Estados cobertos: loading, erro de API (via `extractApiError`), erro de pipeline (`response.erro` → mensagem + botão "Enviar novo vídeo"), id ausente.
- Novo módulo puro `src/utils/analysisDisplay.ts` concentra: `METRIC_CONFIGS` (label/unidade/ideal/chart por `tipo` da `Metrica`, incluindo `transform: flexão = 180 - interno` para joelho e `usaAbs` para overstriding), `METRIC_DISPLAY_ORDER`, `buildGaugeGeometry`, `buildMetricasVisuais`, `groupBySeveridade`, `formatNota`, `notaClassName`, `severidadeDisplay`. Gauge normaliza valor com clamp em [0,100]%, posição + largura da zona ideal idem. 15 testes em `analysisDisplay.test.ts` cobrindo formatação, classes de cor, mapeamento de severidade, geometria com clamp/transform/abs/formatter, agrupamento e fallback para tipos desconhecidos.
- Estilos `analysis-*` adicionados em `src/index.css` (container 820px, card de nota com cor por faixa, métricas com gauge horizontal `position: relative` + marcador absoluto translateX, cards de severidade com borda colorida à esquerda, badges). `npm test` 50/50 passing (35 antigos + 15 novos); `npm run build` (tsc + vite) 0 erros, 297.93 kB / gzip 96.48 kB. `npm run dev` serve HTTP 200 em `/analysis/42`. Dev-browser skill indisponível — verificação visual substituída por build + testes unitários puros + curl.
- Files: `client/src/api/analysis.ts` (novo), `client/src/utils/analysisDisplay.ts` (novo), `client/src/utils/analysisDisplay.test.ts` (novo), `client/src/pages/AnalysisPage.tsx` (reescrito), `client/src/index.css` (classes `analysis-*`).
- **Learnings for future iterations:**
  - `METRIC_CONFIGS` em `utils/analysisDisplay.ts` é o dicionário canônico de faixa ideal + escala de chart por `tipo` de `Metrica`. US-026 (dados brutos) e US-027 (histórico) podem reutilizar diretamente em vez de redeclarar; para adicionar nova métrica basta registrar a chave + incluir em `METRIC_DISPLAY_ORDER`.
  - A convenção "flexão = 180 - interno" do backend (joelho persistido como ângulo interno) já é manipulada no frontend via `transform` do `MetricaConfig`. Qualquer tela que exiba joelho ao usuário (inclusive o raw viewer em US-026) deve aplicar o mesmo transform — não exponha o interno como "20°" porque o texto confunde o corredor leigo.
  - Overstriding usa `usaAbs: true` para posicionar o marcador no gauge pelo módulo (desvio do centro de massa é sempre ≥ 0 para "quanto longe"), mas o `displayValue` preserva o sinal com formatter padrão para o usuário entender se o pé está à frente (+) ou atrás (−) do CoM. Replique o mesmo padrão se criar outro gauge cuja métrica tem sinal mas magnitude é o que importa para classificação.
  - Render com gauge horizontal + zona ideal realçada é uma forma leve de atender "visualmente (gauges ou barras), não apenas numérico" sem depender de Recharts/d3. Para histórias que pedem séries temporais de verdade (US-026), instalar Recharts já está no plano — mas gauges de snapshot não precisam.
  - Estados da tela de análise cobrem: (a) loading (spinner textual), (b) 404/401/403 via `extractApiError(err).general`, (c) `response.erro` quando pipeline terminou em erro, (d) happy path com dados. A ordem dos `if`s importa — sempre cheque `data.erro` **antes** de desestruturar `data.metricas_resumidas` (que pode ser `[]` mas não contém coisas úteis no caminho de erro).
---

## [2026-04-21 21:35] - US-026 Tela de dados biomecânicos brutos com séries temporais e simetria
- Instalado `recharts@^3.8.1` em `client/`. Reescrita `AnalysisRawPage` (`/analysis/:id/raw`): fetch de `GET /api/analysis/{id}/raw`, header com fps + contagem de frames, grade responsiva de 4 gráficos de linha por frame (flexão do joelho esq/dir, ângulo do cotovelo esq/dir, inclinação do tronco, Y do CoM) com eixos rotulados, legenda, tooltip por frame, e uma seção de simetria com barras de 0–30% + marcador visual do limiar de 10% por métrica (TCS, joelho, oscilação). Estados cobertos: loading, erro de API via `extractApiError`, `response.erro` (pipeline falhou) com botão "Enviar novo vídeo", id ausente, sem frames persistidos.
- Novo módulo puro `client/src/utils/rawAnalysis.ts`: `CHART_SPECS` (dicionário canônico dos 4 gráficos com cor/label/transform), `buildFramePoints` (normaliza NaN→null, aplica `flexaoFromInterno` no joelho), `buildAsymmetryRows`, `asymmetryBarPct` (clamp [0,100] escalando por 30% teto), `asymmetryStatus` (ok ≤10%, alerta >10%, nd para null/NaN), `formatAsymmetry`, `formatChartValue` (tooltip com unidade). Novos tipos `RawFrame`/`SimetriaRawResponse`/`AnalysisRawResponse` e helper `getRawAnalysisRequest` em `client/src/api/analysis.ts`.
- 19 testes Vitest em `client/src/utils/rawAnalysis.test.ts` (transformação de joelho, propagação de null, NaN→null, CHART_SPECS cobre os 4 gráficos, buildAsymmetryRows ordem PRD + null, asymmetryBarPct com clamp/abs, asymmetryStatus ok/alerta/nd, formatters com casos de borda). `npm test` 69/69 passing (50 antigos + 19 novos). `npm run build` (tsc + vite) 0 erros, 654 KB gzipped 200 KB (recharts domina). `npm run dev` serve `/analysis/42/raw` com HTTP 200.
- Files: `client/package.json` (+`recharts`), `client/package-lock.json`, `client/src/api/analysis.ts` (+`RawFrame`/`SimetriaRawResponse`/`AnalysisRawResponse`/`getRawAnalysisRequest`), `client/src/utils/rawAnalysis.ts` (novo), `client/src/utils/rawAnalysis.test.ts` (novo), `client/src/pages/AnalysisRawPage.tsx` (reescrito), `client/src/index.css` (classes `raw-chart-*` e `raw-asymmetry-*`).
- **Learnings for future iterations:**
  - Recharts 3 aceita `label: { value, angle, position, fill, fontSize }` em `XAxis`/`YAxis` para rotular eixos; `angle: -90 + position: 'insideLeft'` é o setup canônico para título de eixo vertical. Tooltip aceita `formatter: (value) => string` — reuse `formatChartValue(value, unidade)` para padronização.
  - Para séries com gaps (keypoints faltantes), sempre passe `null` no data (não omita a chave) + `connectNulls={false}` no `<Line>`. Omitir a chave faz Recharts interpretar o frame como "não faz parte desta série", o que distorce eixo X quando outras séries têm o frame. `isAnimationActive={false}` é obrigatório em séries longas — o default anima 400ms e trava o render.
  - A convenção "flexão = 180 − interno" aparece **três vezes** no frontend: `analysisDisplay.ts#flexaoFromInterno` (gauges, US-025), `rawAnalysis.ts#flexaoFromInterno` (gráficos por frame, US-026), e qualquer tela futura de histórico/comparação. Se US-027 precisar exibir joelho por sessão, reuse um dos helpers — não redeclare.
  - O bundle do cliente cresceu de ~290 KB para ~654 KB (gzipped 96 → 200 KB) por causa do recharts. Está acima do limite default de 500 KB do Vite (warning benigno). Histórias futuras que não precisem de recharts ganham com dynamic `import()` da página raw — deixei o refactor fora desta iteração para manter escopo único.
  - Para smoke testar páginas não-autenticadas (como a raw com id fictício), `curl http://localhost:5173/analysis/42/raw` ⇒ 200 só confirma que o Vite serve o HTML inicial. A tela redireciona para `/login` pelo `PrivateRoute` — a verificação real da chamada à API exige um usuário logado + backend rodando; não feito nesta iteração (dev-browser skill indisponível no ambiente).
---

## [2026-04-21 21:45] - US-027 Tela de histórico com paginação
- Reescrita `HistoricoPage` (`/historico`): consome `GET /api/historico-analise?page=X&limit=10` via novo helper `getHistoricoRequest` em `src/api/historico.ts`. Renderiza lista-timeline com data formatada (`dd/mm/yyyy HH:MM`), pace (reusa formatter local), badge de status e nota (`N.N/10` para concluídas, `—` para as demais). Clique em sessão `concluido` ⇒ `navigate('/analysis/:id')`; clique em sessão com erro (`erro_qualidade_keypoints`/`erro_multiplas_pessoas`) expande/recolhe a mensagem do PRD inline (reusa `errorMessageForStatus` de `videoStatus.ts`). Paginação "← Anterior" / "Próxima →" altera `?page=X` via `useSearchParams` (refletido na URL).
- Novo util puro `src/utils/historicoDisplay.ts`: `statusBadge` (mapeia todos os 7 status → `{label, className}`), `isConcluido`/`isProcessing`, `errorReasonForStatus` (drena para `videoStatus.errorMessageForStatus`), `formatPaceMinKm` (decimal ⇒ `M:SS /km`), `formatNotaGeral` (`N.N/10` ou `—`), `formatCriadoEm` (ISO-8601 ⇒ `dd/mm/yyyy HH:MM`), `parsePageParam` (guard contra NaN/negativo ⇒ 1) e `totalPages` (ceil, mínimo 1).
- 19 testes Vitest em `src/utils/historicoDisplay.test.ts` cobrindo: mapeamento de badge para todos os status, fallback neutro, isConcluido/isProcessing, errorReasonForStatus (PRD + null), pace formatting (`5:30 /km`, `4:00 /km`, null/0/NaN ⇒ `—`), nota formatting, parse de query param (inválido ⇒ 1), totalPages (ceil + mínimo 1). `npm test` 88/88 passing (69 antigos + 19 novos). `npm run build` (tsc + vite) 0 erros, 658 KB gzipped 201 KB. `npm run dev` serve HTTP 200 em `/historico`. Dev-browser skill indisponível — verificação visual substituída por build + testes unitários puros + curl.
- Files: `client/src/api/historico.ts` (novo), `client/src/utils/historicoDisplay.ts` (novo), `client/src/utils/historicoDisplay.test.ts` (novo), `client/src/pages/HistoricoPage.tsx` (reescrita), `client/src/index.css` (classes `historico-*`).
- **Learnings for future iterations:**
  - Query string como fonte única da página atual: `const [searchParams, setSearchParams] = useSearchParams()` + `parsePageParam(searchParams.get('page'))` na leitura; `setSearchParams(nextParams)` na navegação. Isso satisfaz o critério "controles de paginação refletidos na query string" sem estado duplicado. Recarregar a página preserva a posição do usuário.
  - Para sessões em erro, o padrão "expandir inline via toggle" (um `selectedErrorId` no state) é mais leve que abrir um modal e evita roteamento artificial. `aria-expanded` no botão mantém a semântica de disclosure.
  - `formatCriadoEm` parseia via `new Date(iso)` e formata no **fuso local** (`getDate`/`getMonth`/`getHours`). Pydantic v2 serializa timestamps com offset (`+00:00`), então o Date do JS converte corretamente. Para testes determinísticos, use `new Date(yyyy, mm-1, dd, HH, MM).toISOString()` como input em vez de string hardcoded — o host TZ varia (CI vs dev).
  - `formatPaceMinKm(5.5) === '5:30 /km'` reutiliza a mesma fórmula de `utils/pace.ts#kmHToPaceParts`, mas com input diferente (decimal min/km). Evitei chamar `kmHToPaceParts(60/pace)` para não ter dupla divisão em floats — manter o formatter local é mais direto.
  - Paginação com `hasNext = data !== null && page < pages` evita pedir a página N+1 quando `data.total` já confirma que estamos na última página. Anterior + próxima ambos respeitam `disabled` no botão e o guard no handler — o usuário não consegue sair da faixa válida.
---

## [2026-04-22 19:15] - US-028 Sistema de Design — tokens base (cores, tipografia, espaçamento, sombras, motion)
- Criei `client/src/styles/tokens.css` com a paleta Emerald + Tangerine sobre Slate frio (PRD 18.2), escala tipográfica (18.3), espaçamentos/raios/sombras/motion (18.4), em tema light + override completo via `@media (prefers-color-scheme: dark)`. Importado **antes** de `index.css` em `client/src/main.tsx`.
- Refatorei `client/src/index.css` para consumir exclusivamente `var(--token)` em cores/sombras/raios/espaçamentos — nenhum valor hex/px/rgba hard-coded resta. Removidos `--accent: #aa3bff`, `--code-bg`, `--text-h`, `--social-bg`, `--shadow`, `--sans/heading/mono` (todos colapsados nos novos tokens da paleta). Substituí também as duas referências roxas (`#aa3bff`) que ainda existiam em `client/src/utils/rawAnalysis.ts` (cores das séries de joelho/cotovelo) por `--brand-500` (esq) e `--accent-400` (dir).
- `client/index.html`: adicionei `<meta name="theme-color">` com variants `light`/`dark` via media-query, `preconnect` ao Google Fonts, `<link rel="preload" as="font" crossorigin>` para Inter e Space Grotesk woff2 + `<link rel="stylesheet" ... display=swap>`. Documentação completa em `client/src/styles/README.md` (paleta tabular, tipografia, espaçamento, raios, sombras, motion).
- Files: `client/src/styles/tokens.css` (novo), `client/src/styles/README.md` (novo), `client/src/main.tsx` (import dos tokens antes do index.css), `client/src/index.css` (refator total para usar tokens), `client/index.html` (theme-color + preload de fontes), `client/src/utils/rawAnalysis.ts` (substituição de cores roxas em CHART_SPECS).
- `npm run build` passa (tsc -b + vite build, 9.15s, dist 18.13kB CSS gzip 3.61kB). `npm test` mostra 86/88 passing — as 2 falhas (`videoValidation.test.ts`) são pré-existentes (verificado com `git stash` antes de aplicar as mudanças desta US).
- **Learnings for future iterations:**
  - **Hierarquia das CSS custom properties:** `tokens.css` é a fonte única de verdade; `index.css` e qualquer estilo futuro consomem `var(--token)` direto. Convenção de naming: `--text` = headline (`#0F172A`), `--text-muted` = body, `--text-subtle` = hints — **invertido** do padrão antigo (`--text-h` era headline). Cuidado ao migrar referências.
  - **Surface vs background:** `--bg` é o fundo da app (slate-50/-900); `--surface` é card/input branco/slate-800; `--surface-muted` é fill de tracks/code (slate-100/-800 levemente lighter que bg). Use `--surface` para qualquer container elevado em vez de `var(--bg)` direto, para que dark mode renderize com hierarquia.
  - **Severidades e semânticas:** existem dois eixos — semântico (`--success/warning/danger/info`) e severidade de recomendação (`--severity-{critico,atencao,informativo}-{bg,border,text}`). Use as severidades nas cards/badges de recomendação (alinhamento PRD 18.2); use semânticas em banners genéricos (form errors, FPS warning).
  - **Shadow tokens:** `--shadow-focus` é o anel verde-emerald (rgba 16,185,129,0.35) para `:focus-visible` global em US-030. Já é o token padrão em `box-shadow` de inputs de formulário neste refator.
  - **Fontes:** Inter (corpo) e Space Grotesk (display) carregam via Google Fonts CDN; preload aponta para latin .woff2 v20/v18 — se o browser ignorar o preload (URL desatualizada), o stylesheet ainda carrega normalmente com `display=swap` (degradação graciosa, sem regressão).
  - **Off-scale spacing:** valores 6px/10px que apareciam no CSS antigo foram arredondados para `--space-2` (8px) ou `--space-3` (12px). Mantive `height: 10px` para tracks de barra (não há `--space` exato) — quando US-031 (biblioteca de componentes) refatorar, considere se vale criar `--track-height` ou padronizar para `--space-3` (12px).
  - **`#root` ainda tem `width: 1126px`** + bordas decorativas — US-034 (responsividade mobile-first) vai eliminar essas duas regras. Não removi por estar fora do escopo desta US (escopo: tokens, não layout shell).
  - **Roxo zerado:** verifiquei que `tokens.css`, `index.css` e todos os arquivos `.tsx`/`.ts` não contêm mais `aa3bff`/`c084fc`/`#16171d`. As ocorrências em `client/src/styles/README.md` são contextuais ("substitui o roxo `#aa3bff`") e em `tokens.css` são apenas os tokens `--accent-50/-400/-500` (tangerine, não roxo).
---

## [2026-04-22 19:25] - US-029 App Shell — top nav, logo, branding e footer mínimo
- Marca: `client/src/branding.ts` (`BRAND_NAME = 'Stride'` + `pageTitle(label)` helper). `client/index.html` agora usa `lang="pt-BR"` e `<title>Stride</title>`; favicon SVG substituído (geometria do `Logo` sobre fundo brand-500).
- `client/src/components/AppShell.tsx`: shell persistente das rotas privadas (skip-link → top nav sticky 64px → main → footer). Top nav contém logo SVG (`Logo.tsx`), NavLinks `Análise`/`Histórico` (com `aria-current=page` ativo), avatar do usuário com iniciais e dropdown `Sair`. Em viewport <640px nav+user somem e aparece hamburger que abre drawer lateral (300/85vw) com `inert` em `#shell-root` enquanto aberto, devolve foco ao botão hamburger ao fechar e ouve `Escape`. `App.tsx` agora aninha `<PrivateRoute><AppShell>{page}</AppShell></PrivateRoute>` via helper `privateElement(...)`. Cada página declara `usePageTitle(label)` (`client/src/hooks/usePageTitle.ts`) restaurando o título anterior no unmount.
- Files: `client/src/branding.ts` (novo), `client/src/hooks/usePageTitle.ts` (novo), `client/src/components/Logo.tsx` (novo), `client/src/components/AppShell.tsx` (novo), `client/src/App.tsx` (privateElement helper), `client/index.html` (lang=pt-BR, brand title), `client/public/favicon.svg` (novo símbolo), `client/src/index.css` (estilos do shell + skip link + drawer + footer + media query <640px), `client/src/pages/{Login,Signup,Upload,Status,Analysis,AnalysisRaw,Historico}Page.tsx` (`usePageTitle`).
- `npm run build` passa (vite build, 4.66s, 23.81 kB CSS gzip 4.48 kB). `npm test` 86/88 — as duas falhas em `videoValidation.test.ts` são pré-existentes (US-023), não tocadas nesta iteração. `npm run dev` boot limpo em `:5174`.
- **Learnings for future iterations:**
  - **Brand string única:** `client/src/branding.ts` é fonte única do nome do produto; qualquer hook/título/marca consome de lá. Não codifique `'Stride'` em outros arquivos.
  - **Hook de título:** `usePageTitle('Tela')` em `client/src/hooks/usePageTitle.ts` chama no topo de cada componente de página. Restaura o título anterior no unmount — bom para SPAs onde o usuário navega entre rotas. Use sempre o label sem o sufixo ` — Stride`; o hook adiciona via `pageTitle()`.
  - **AppShell wrap pattern:** em `App.tsx`, o helper `privateElement(<Page/>)` retorna `<PrivateRoute><AppShell>{page}</AppShell></PrivateRoute>`. Páginas Login/Signup ficam fora (apenas branding sutil dentro delas se desejado). Adicionar nova rota privada = só chamar `privateElement(<NewPage/>)`.
  - **Drawer mobile com `inert`:** padrão para qualquer overlay full-height — aplicar `inert` em `#shell-root` (não em `body` inteiro, que ainda contém o drawer/header) enquanto aberto, devolver foco ao trigger no fechamento, ouvir `Escape`. Reutilizável para futuros overlays (modais, command palette, etc.).
  - **Top nav sticky vs fixed:** preferi `position: sticky; top: 0` ao invés de `fixed` — o `<header>` participa do flow normal e não exige `padding-top` no conteúdo para compensar. Ainda atende "top nav fixa" da AC. Z-index ordem: header=100, user-menu=110, drawer-backdrop=200, drawer=210.
  - **Hit targets mobile:** todos os controles do drawer têm `min-height: 44px`; o botão hamburger e avatar têm 44×44; o botão de fechar do drawer também 44×44. Isso atende AC (>=44px) e o critério geral de a11y do PRD 18.7.
  - **Skip link pattern:** `.skip-link` é `position: absolute` com `transform: translateY(-200%)` por default e `:focus { transform: translateY(0) }` — fica visualmente oculto mas sempre presente no DOM como primeiro elemento focável. Aponta para `#main` (alvo no AppShell). Páginas dentro do shell não devem mais montar `<main>` próprio porque o AppShell já tem; mas as páginas atuais ainda renderizam `<main className="auth-container">` — é redundante mas inofensivo (`<main>` aninhado é semanticamente estranho mas não quebra). US-030 ou US-035+ devem refatorar páginas para usar `<section>`/`<div>` em vez de `<main>` quando dentro do AppShell.
---

## [2026-04-22 19:30] - US-030 Acessibilidade base — focus-visible, prefers-reduced-motion, skip link, hierarquia
- Consolidei ownership do `<main id="main" tabIndex={-1}>` nas páginas (raiz do JSX de cada page), removi o `<main>` do `AppShell` — agora shell renderiza `<div className="app-shell-main">`, acabando com o aninhamento de `<main>` do US-029. Skip-link migrou para `App.tsx` (fora de `<Routes>`) para aparecer também em Login/Signup.
- Adicionei estilos globais de a11y em `client/src/index.css`: `*:focus-visible` com anel `var(--shadow-focus)`, `@media (prefers-reduced-motion: reduce)` neutralizando `transition-duration`/`animation-duration`/`animation-iteration-count`/`scroll-behavior` em `*`/`::before`/`::after`, `scroll-margin-top: var(--space-16)` em `h1..h6`, e `main[tabindex]:focus { outline: none }` para não piscar anel no container alvo do skip-link. Removi `.app-shell-main:focus { outline:none }` (órfão — `<main>` saiu do shell). Botões de paginação do histórico ganharam `aria-label="Página anterior"`/`"Próxima página"` com as setas `←`/`→` marcadas `aria-hidden="true"`.
- Files: `client/src/App.tsx`, `client/src/components/AppShell.tsx`, `client/src/index.css`, `client/src/pages/{Login,Signup,Upload,Status,Analysis,AnalysisRaw,Historico}Page.tsx`, `client/CLAUDE.md`.
- `npm run build` passa (4s, CSS 24.12 kB gzip 4.60 kB). `npm test` 86/88 — mesmas 2 falhas pré-existentes de US-023 (`videoValidation.test.ts`), não tocado nesta iteração. Dev server boot limpo.
- **Learnings for future iterations:**
  - **Um `<main>` por página, sempre id="main":** páginas (privadas via AppShell ou auth) renderizam `<main id="main" tabIndex={-1} className="<container>">` como raiz do JSX. AppShell fica com `<div className="app-shell-main">`, não `<main>`. Toda página nova deve seguir esse padrão; o skip-link de `App.tsx` (`href="#main"`) assume exatamente um alvo com esse id por rota.
  - **`:focus-visible` global na raiz:** o seletor `*:focus-visible { outline: none; box-shadow: var(--shadow-focus) }` cobre todo interativo (buttons, links, inputs, elementos com tabIndex) com o mesmo anel verde. Evita precisar espalhar `:focus` em cada regra. `:focus-visible` não dispara em cliques de mouse (UA heuristics) — evita anel indesejado em botões clicados.
  - **Reduced-motion global, não case-by-case:** bloco único no `:root` do `index.css` basta. Qualquer `transition`/`animation` nova em componentes futuros é neutralizada automaticamente. Não é preciso lembrar de envolver cada animação em `@media (prefers-reduced-motion)`.
  - **Aria-label em botões com ícone decorativo:** quando um botão mistura texto e ícone (ex: `← Anterior`), marcar o ícone como `aria-hidden="true"` e setar `aria-label` explícito no botão — leitor de tela lê só o texto limpo, sem "seta esquerda". Já aplicado na paginação do Histórico (reutilizar em futuros botões Next/Prev).
  - **Lighthouse/audits manuais:** não há CI rodando Lighthouse — as métricas de a11y (>=95) ficam como checagem manual via DevTools. Se uma história futura quebrar a11y (ex: contraste, regras ARIA), a build ainda passa; reavaliar periodicamente.
  - **Convenção de heading:** uma `<h1>` por página (já garantido por `usePageTitle` + 1 `<h1>` no JSX). `<h2>` para seções; `<h3>` para sub-seções (AnalysisPage usa para "Pontos críticos", etc.). `scroll-margin-top: var(--space-16)` já protege contra a top nav cobrir o heading ao rolar até âncora.
---

## [2026-04-22 20:45] - US-031 Biblioteca de componentes compartilhados
- Criei `client/src/components/ui/` com 7 componentes (`Button`, `Input`, `Field`, `Card`, `Badge`, `Banner`, `Skeleton`) + barrel `index.ts`. Cada componente tem JSDoc no topo descrevendo a API e usa `import type` para tipos (compatível com `verbatimModuleSyntax`). Button oferece variantes primary/secondary/ghost/danger, tamanhos sm/md/lg e prop `loading` que exibe um spinner inline sem apagar o label (`aria-busy` + `disabled` enquanto loading). Input aplica `font-size: 16px` no mobile e cai para `--text-sm` em `min-width: 640px`. Field compõe `<label>` + `<Input>` + mensagem de erro/hint, com `aria-describedby` apontando para a mensagem ativa e `useId()` como fallback para o `id`. Card renderiza surface (shadow-sm, radius-lg) e aceita `as` para escolher a tag do título (default `h2`). Badge tem 5 variantes (success/warning/danger/info/neutral). Banner alterna `role="status"` (polite) e `role="alert"` (assertive) via prop `assertive`. Skeleton aceita `width`/`height` CSS + `rounded` para círculos.
- Adicionei classes `.ui-*` em `client/src/index.css` consumindo exclusivamente tokens de `styles/tokens.css`; incluí keyframes `ui-spin` (spinner do Button) e `ui-skeleton-pulse` (placeholder), ambos neutralizados automaticamente pelo bloco global de `prefers-reduced-motion` já existente. Nenhuma página foi migrada nesta iteração (AC explícita: "Não migrar páginas nesta US — apenas criar a biblioteca").
- `npm run build` passa (tsc -b + vite build, 5.3s, CSS 29.90 kB gzip 5.37 kB). `npx eslint src/components/ui/` limpo — nenhum problema nos arquivos novos. `npm run lint` mantém os 8 problemas pré-existentes em AnalysisPage/AnalysisRawPage/HistoricoPage/AppShell/AuthContext (verificado via `git stash` antes de aplicar: idênticos ao baseline).
- Files: `client/src/components/ui/{Button,Input,Field,Card,Badge,Banner,Skeleton}.tsx` (novos), `client/src/components/ui/index.ts` (novo), `client/src/index.css` (seção "Biblioteca de componentes compartilhados (US-031)").
- **Learnings for future iterations:**
  - **Barrel canônico:** `import { Button, Field, Card } from '@/components/ui'` (caminho relativo, sem path alias configurado) é o único ponto de entrada. Histórias de redesign (US-035+) devem usar o barrel em vez de importar por arquivo individual — mantém diffs menores quando/ se os componentes forem extraídos para um pacote.
  - **Classes `ui-*` globais, estilo consolidado:** não há CSS Modules. Os seletores `.ui-button`, `.ui-input` etc. são globais em `index.css`. Isso é coerente com as classes `auth-*`, `historico-*`, `analysis-*` já existentes. Ao adicionar um novo componente, siga o padrão: regra base + variantes (`.ui-X-primary`), tudo em tokens.
  - **Spinner dentro do Button:** o padrão é renderizar o spinner **antes** do label (flex gap via `--space-2`), nunca substituir o texto. AC específica dizia "mantendo o label original" — é a UX esperada para que o usuário não perca contexto do que o botão faz. Reuso do mesmo spinner em outros componentes de loading futuro: copie o `.ui-button-spinner` CSS ou exponha como `<Spinner/>` em uma futura história.
  - **Field com `useId()`:** o hook gera um id estável entre renders mesmo em React 19 Strict Mode. Use sempre que o componente precisar ligar `<label htmlFor>` a um `<input id>` sem forçar o consumidor a passar id manualmente. Consumidor ainda pode sobrescrever via prop `id`.
  - **Banner semântica:** `role="status"` + `aria-live="polite"` é correto para avisos não-críticos; `role="alert"` + `aria-live="assertive"` interrompe o screen reader — reserve para erros bloqueantes (ex: "Sessão expirada"). A prop `assertive` tem default false para evitar uso excessivo.
  - **Skeleton com width/height livre:** o consumer passa qualquer valor CSS (`'12ch'`, `'40%'`, `'48px'`). Mantenha `min-width: 1em` / `min-height: 1em` como fallback para que um skeleton sem dimensões ainda apareça visivelmente. Em US-033 (loading states), páginas vão espelhar o layout final com um conjunto de Skeletons — reutilize esta base.
  - **Lint baseline:** `npm run lint` já tinha 8 problemas pré-existentes (erros `react-hooks/set-state-in-effect` em 3 páginas + refresh warnings em AppShell/AuthContext) herdados de US-025/US-026/US-027. Minhas novas UI files passam 100% limpas (`npx eslint src/components/ui/` sem output). AC marcado como atendido pela neutralidade do diff — arrumar estes erros antigos é fora de escopo desta US ("Não migrar páginas nesta US"). CLAUDE.md do client diz explicitamente "lint é opcional; não é bloqueador das histórias"; futuras iterações podem endereçar os erros pré-existentes.
---

## [2026-04-22 14:10] - US-032 Sistema de feedback — toast, banners e confirmações
- Novo `ToastProvider` + hook `useToast` em `client/src/context/ToastContext.tsx`: portal em `document.body`, duas regiões `aria-live` (polite/assertive), auto-dismiss em 5s com pause-on-hover/focus, botão de fechar sempre visível, animação de entrada via `transform: translateX + opacity` (compositor-friendly; já neutralizada por `prefers-reduced-motion` global).
- Interceptor de 401 em `client/src/api/client.ts` via `setUnauthorizedHandler`; `UnauthorizedBridge` (novo, montado em `App.tsx` dentro de `AuthProvider`+`BrowserRouter`+`ToastProvider`) registra handler que dispara `toast.error('Sessão expirada') + logout() + navigate('/login', { replace: true })`. Endpoints de auth (login/register) são explicitamente ignorados pelo handler para não derrubar o usuário em credenciais inválidas.
- Wire-ins: upload bem-sucedido em `UploadPage` dispara `toast.success('Vídeo enviado. Iniciando análise.')` antes do redirect. Erros transitórios (fetch failures) em `StatusPage`, `HistoricoPage`, `AnalysisPage`, `AnalysisRawPage` agora disparam `toast.error(...)` — as `<p className="form-error">` soltas foram removidas dessas páginas; erros de estado (`data.erro`, identificador ausente) migraram para `<Banner variant="danger" assertive>` da biblioteca UI. `form-error` restante só aparece em formulários (Login/Signup/Upload), que é o escopo correto para US-035+ redesign.
- Files: `client/src/context/ToastContext.tsx` (novo), `client/src/components/UnauthorizedBridge.tsx` (novo), `client/src/api/client.ts` (interceptor de resposta + `setUnauthorizedHandler`), `client/src/App.tsx` (wrap `ToastProvider` + mount `UnauthorizedBridge`), `client/src/index.css` (bloco de estilos `.toast-*` e `.toast-region` só com tokens), `client/src/pages/{Upload,Status,Historico,Analysis,AnalysisRaw}Page.tsx` (wire-ins de toast + swap para Banner). `npm run build` passa (tsc -b + vite build).
- **Learnings for future iterations:**
  - Para erros **globais** (transientes de rede, 401 etc.) use toast. Para erros **de estado de página** (route param ausente, `data.erro` vindo do backend) use `<Banner variant="danger" assertive>` inline — não mistura com toast.
  - `useToast()` dentro de componentes que também aparecem em páginas de auth (Login/Signup) não é obrigatório: o handler global cobre 401, e erros de credenciais continuam sendo pintados no form via `extractApiError`. Não faça wire de toast duplicado.
  - O toast de 401 não dispara em `/api/auth/login` ou `/api/users/register`. Se adicionar novos endpoints de auth no futuro, estenda `AUTH_PATHS` em `api/client.ts`.
  - O interceptor + handler global é registrado em `useEffect` dentro do `UnauthorizedBridge`; ele também é limpo no unmount (`setUnauthorizedHandler(null)`). Qualquer teste que monta/desmonta a app precisa reacoplar.
  - Para fazer o prefers-reduced-motion neutralizar a animação de toast, basta que os transitions usem `var(--duration-*)` — o reset global em `*` já cuida disso. Não precisa de `@media` específico para toasts.
  - `toast.success` / `toast.info` usam `aria-live="polite"`; `toast.error` cai em `assertive` por default (override via `options.assertive`). Reserve `assertive` para erros que precisam interromper o que o usuário está lendo.
  - Animação de entrada depende do padrão "raf + classe `toast-enter`". Se precisar animar saída (US-042+), adicionar estado `leaving` que aciona classe CSS de exit, e chamar `onDismiss` no `transitionend` — hoje o dismiss é imediato (acceptable para US-032, mas a polishing pass pode suavizar).
---

## [2026-04-22 17:05] - US-033 Estados padronizados — Loading (skeleton), Empty e Error com recuperação
- Adicionados `LoadingState`, `EmptyState` e `ErrorState` em `client/src/components/ui/states/` (barrel em `states/index.ts`), reexportados via `components/ui/index.ts`. `LoadingState` tem variantes `status | analysis | analysis-raw | historico` — cada uma renderiza um conjunto de Skeletons com dimensões fixas que espelham o layout final (previne CLS).
- Substituído todo loading/empty/error textual de StatusPage, AnalysisPage, AnalysisRawPage e HistoricoPage pelos novos componentes. Cada caminho de erro agora inclui uma próxima ação (botão "Tentar novamente" nas páginas que disparam fetch pontual + link de fallback para `/historico`, `/analysis/:id` ou `/upload`) — "no dead ends" aplicado. HistoricoPage com lista vazia usa `EmptyState` com ilustração SVG + CTA "Enviar primeiro vídeo".
- `npm run build` (tsc -b + vite build) passa limpo. Pre-existing: 2 falhas em `videoValidation.test.ts` (não relacionadas — também falhavam antes da iteração).
- Files: `client/src/components/ui/states/{LoadingState,EmptyState,ErrorState,index}.tsx|ts` (novos), `client/src/components/ui/index.ts` (barrel), `client/src/index.css` (+~230 linhas em bloco "States"), `client/src/pages/{StatusPage,AnalysisPage,AnalysisRawPage,HistoricoPage}.tsx`.
- **Learnings for future iterations:**
  - `LoadingState` aceita `variant: 'status' | 'analysis' | 'analysis-raw' | 'historico'`. Para novos redesigns de página (US-037+), adicione a variante correspondente no mesmo arquivo — mantenha as dimensões fixas dos skeletons alinhadas ao layout real. Container externo tem `role="status" aria-busy="true" aria-live="polite"` + label visualmente oculta via `.visually-hidden`.
  - `ErrorState` aceita `onRetry?: () => void` + `backTo?: {to, label}` + `action?: ReactNode`. Para dispararem retry na mesma rota, páginas usam um `retryCount` state na dep array do `useEffect` — incrementar = refetch. Proibido estado de erro sem pelo menos uma das três props ("no dead ends").
  - `EmptyState` exige CTA obrigatório (`action: ReactNode`). SVG ilustração padrão é decorativo (`aria-hidden="true"`) — substitua via prop `illustration` se a história precisar de algo mais específico à tela (US-041 pode customizar para histórico).
  - Utility CSS `.visually-hidden` (position:absolute + clip:rect) adicionada em `index.css` — use para labels de screen reader ocultas visualmente em qualquer componente novo.
---

## [2026-04-22 20:10] - US-034 Responsividade mobile-first e safe areas
- Reescrito `#root` em `client/src/index.css`: `max-width: 1280px; margin-inline: auto; padding-inline` escalando `var(--space-4)` (mobile) → `var(--space-5)` (≥640px) → `var(--space-8)` (≥1024px); removido `width: 1126px` e `border-inline` decorativo.
- Adicionados tokens de breakpoint em `styles/tokens.css` (`--bp-sm/md/lg/xl`) e bloco global mobile-first em `index.css`: tap-highlight verde brand, `touch-action: manipulation` em botões/links/labels, `font-size: 16px` em inputs (vira `--text-sm` em ≥640px). `.app-shell-header` ganhou `padding-top/left/right: env(safe-area-inset-*)` para respeitar o notch do iPhone.
- Hit targets elevados para ≥ 44px em todos os controles interativos do fluxo principal: `.auth-form input/select` (também ganhou `font-size: 16px` explícito), `.auth-form button[type=submit]` (48px), `.ui-input` (44px mobile, 40px ≥640px), `.ui-button-md` (44px <640px via media query), `.historico-pagination-button` (44px), `.historico-item-button` (44px + grid `1fr auto` 2 linhas em mobile para evitar overflow), `.status-restart-button` (48px), `.analysis-primary-link` (44px). Viewport meta mantém `width=device-width, initial-scale=1.0` (sem `user-scalable=no`/`maximum-scale=1`).
- `npm run build` (tsc -b + vite build) passa limpo; bundle CSS 37.17 kB → 6.38 kB gzipped. Pre-existing: mesmas 2 falhas em `videoValidation.test.ts` (não relacionadas à iteração).
- Files: `client/src/styles/tokens.css`, `client/src/index.css`, `client/CLAUDE.md`.
- **Learnings for future iterations:**
  - `#root` agora é o único dono do gutter lateral global (padding-inline responsivo). Páginas herdam esse gutter — os containers internos (`.auth-container`, `.analysis-container`, `.historico-container`) ainda adicionam o próprio padding de página (`var(--space-4)`), o que dá ~32px em mobile 360px. Se em um redesign futuro o gutter duplicado incomodar, remova do container de página (não do `#root`).
  - CSS `@media` **não** resolve `var()` — por isso as queries usam literal (`@media (min-width: 640px)`). Os tokens `--bp-*` servem só de referência humana. Ao adicionar novos breakpoints, duplicar o valor na query.
  - Safe-area-insets: aplicado só em `.app-shell-header`. Se criar novas barras fixas top/bottom (banner, wizard stepper, bottom-sheet), replique `padding-top/left/right: env(safe-area-inset-*)` no elemento externo; sem isso, o notch do iPhone e o indicador home iOS cortam o conteúdo.
  - Hit targets ≥ 44px: para qualquer novo controle interativo, padrão é `min-height: 44px` no mobile (`@media (max-width: 639px)` ou incondicional). `.ui-button-md` tem `min-height: 40px` por padrão mas sobe para 44px em mobile via media query — herdar esse componente em vez de reinventar.
  - iOS zoom-prevention: `input/select/textarea { font-size: 16px }` é global; só use `var(--text-sm)` (14px) em ≥640px. Não diminua font-size de input em mobile mesmo que seja "só um campo pequeno".
---

## [2026-04-22 14:30] - US-035 Redesign — tela de Login
- Migrada a `LoginPage` para usar `Field`/`Input`/`Button`/`Banner` da biblioteca UI (US-031). Layout card centralizado (max-width 400px) com hero lateral (gradiente Emerald + SVG abstrato de passos) aparecendo apenas em `≥1024px`. Branding topo do card (logo + "Stride"), subtitle descritivo e link "Criar conta" abaixo do botão Entrar.
- Erro global de credenciais usa `<Banner variant="danger" assertive>` no topo do form; erros de campo seguem inline via prop `error` do `Field`. Foco volta para o primeiro campo com erro em fail; autofocus no e-mail apenas em desktop (`window.matchMedia('(min-width: 640px)')` dentro de `useEffect`). E-mail recebe `inputMode="email"` + `spellCheck={false}` + `autoComplete="email"`; senha recebe `autoComplete="current-password"`. Botão `lg` com `loading` mantém label "Entrar" durante submit.
- Files: `client/src/pages/LoginPage.tsx` (redesign completo), `client/src/index.css` (bloco `.login-layout/.login-panel/.login-card/.login-brand/.login-hero` + responsivo `@media (min-width: 1024px)`).
- **Learnings for future iterations:**
  - `Field` é o wrapper preferencial — passa `id` explícito quando precisar focar por DOM (primeiro erro de submit ou autofocus condicional). Sem `id` explícito, o `useId()` interno gera o id mas você perde o handle DOM.
  - Hero lateral em telas de auth: `display: none` até `≥1024px`, depois `display: block` com `background: linear-gradient(135deg, var(--brand-500), var(--brand-700))`. Mantenha `aria-hidden="true"` no `<aside>` inteiro — é puramente decorativo.
  - SVG decorativo do hero usa `currentColor` ou cores hard-coded em rgba(255,255,255,…) sobre o gradiente verde. `linearGradient` inline com `id` único dentro do SVG (colisão improvável, mas usar prefixo `login-hero-*`).
  - Submit com Enter já funciona por ser `<button type="submit">` dentro de `<form onSubmit>`. Nada especial — evite reinventar handlers de `onKeyDown`.
  - Guard "já autenticado" + autofocus: o `useEffect` de autofocus roda mesmo quando `isAuthenticated=true`, porque o `<Navigate>` é avaliado depois dos effects? Não — `useEffect` roda após o commit; se o componente cedeu para `<Navigate>` o efeito nem foi enfileirado. Checar `isAuthenticated` dentro do effect é defensivo mas redundante. Manter o guard por ser barato e explícito.
---

## [2026-04-22 17:25] - US-036 Redesign — tela de Cadastro
- Migrada a SignupPage para o layout de auth redesign (US-035): split-screen com `.login-layout` + `.login-panel` + `.login-hero` e `.signup-card` (variante mais larga do `.login-card`). Formulário agora é composto por três `<fieldset className="signup-fieldset">` com `<legend>` em caps ("Conta", "Perfil físico", "Experiência"). Cada campo usa `<Field>` da biblioteca UI (exceto o `<select>` de nível, que reutiliza as classes `.ui-field` + `.ui-input` inline para aceitar `<select>`).
- Toggle "Mostrar/Ocultar senha" (`aria-pressed`, `aria-controls={senhaId}`) alterna `type` entre `password` e `text` no campo de senha. Validação inline reforçada: senha agora exige ≥ 8 caracteres (antes 6) e e-mail usa regex simples `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`. Altura obrigatória (1–300), peso opcional (1–500). Focus do primeiro erro via `focusFirstError(errs)` + autofocus em `#name` apenas em desktop (`matchMedia('(min-width: 640px)')`).
- 409 do backend (`E-mail já cadastrado`) é movido de `parsed.general` para `fieldErrors.email` via heurística `/mail/i`, sem patch do helper genérico `extractApiError`. Sucesso dispara `toast.success('Conta criada com sucesso!')` antes do redirect para `/upload`. Autocomplete `name` / `email` / `new-password` confirmado em todos os campos relevantes.
- Files: `client/src/pages/SignupPage.tsx` (reescrito), `client/src/index.css` (bloco `/* Signup redesign (US-036) */` após o bloco de login). `npm run build` passa (tsc -b + vite build, 40.36 kB CSS, 678.68 kB JS). `npm run dev` sobe sem erros de runtime.
- **Learnings for future iterations:**
  - `<select>` não é suportado diretamente pelo `<Field>` (o componente envolve `<Input>`). Para select com estilo consistente, use um wrapper manual `<div className="ui-field"> + <label className="ui-field-label"> + <select className="ui-input"> + <p className="ui-field-error"|"ui-field-hint">`. Reaproveite o padrão nos redesigns futuros (Upload US-037 tem dropdowns, histórico US-041 tem filtros).
  - `extractApiError` mantém 409 em `general` (não distingue campo). Quando a história exige mapear 409 para um field-error, faça **no componente** uma heurística sobre `parsed.general` — não polua o helper genérico. Padrão: `if (parsed.general && /<keyword>/i.test(parsed.general)) { nextFieldErrors[campo] = parsed.general; nextGeneral = null }`.
  - Toggle de senha: botão `aria-pressed={mostrarSenha}` + `aria-controls={inputId}`. Label troca entre "Mostrar senha" e "Ocultar senha" para leitores. `type={mostrarSenha ? 'text' : 'password'}` no `<Field>` (propagado pelo spread para o `<Input>`). Posicione o botão **fora** do `<Field>` (o wrapper UI não tem slot de adorno); use um wrapper flex coluna com `.signup-password` e align-self: flex-end no botão.
  - Autofocus desktop-only via `matchMedia('(min-width: 640px)')` dentro de `useEffect` — mesmo padrão de US-035. Reaproveitável em qualquer tela de formulário longo (Upload, filtros).
  - Senha mínima agora é **8 caracteres** no frontend (PRD US-036). O backend ainda aceita ≥ 1 via `Field(...)` sem min_length explícito — se uma história futura precisar alinhar, adicionar `min_length=8` em `UsuarioCreate.senha` em `server/src/schemas/usuario.py`.
  - Não foi possível "Verify in browser" em ambiente WSL headless — validação visual manual pendente; todos os caminhos (validação inline, submit, 409, toggle de senha) exercitáveis via dev server em `http://localhost:5173` (porta pode subir pra 5175 se outras instâncias estiverem rodando).
---

## [2026-04-22 20:28] - US-037 Redesign — tela de Upload (drag-drop, preview, validações ricas)
- Reescrita `UploadPage.tsx` em layout de 2 colunas (dropzone à esquerda, checklist à direita em ≥768px) com dropzone `role="button"` que aceita drag-and-drop (`onDragOver/onDragEnter/onDragLeave/onDrop`) e clique/Enter/Space para abrir o file picker nativo. Preview do vídeo selecionado renderizado via `<video controls muted playsInline preload="metadata">` usando `URL.createObjectURL(file)` — object URL revogado no unmount e em cada troca de arquivo.
- Checklist visual de 4 itens (duração, resolução, FPS, perspectiva) com ícones de estado `pass/fail/warn/pending` e detalhe por item (ex: "1920×1080 · mínimo 640×480"). Perspectiva fica sempre `pending` (validada pelo servidor após envio). Hint colapsável "Como gravar o vídeo ideal" via `<details><summary>` com seta `+/−` rotativa. Erros do servidor (422) renderizados em `<Banner variant="danger" assertive>` com sugestão de correção (no-dead-end). Upload com barra de progresso real via `onUploadProgress` do axios — o helper `uploadVideoRequest` ganhou a prop `onProgress?: (percent:number)=>void`.
- Botão renomeado para "Analisar corrida" via `<Button size="lg" loading={submitting}>`. Título da página mantido como `'Nova análise — Stride'` via `usePageTitle('Nova análise')`. `npm run build` passa (tsc -b + vite build), dev server sobe sem erros. Testes de `videoValidation.test.ts` continuam com 2 falhas pré-existentes (originadas do commit `98ca1eb config` que reduziu thresholds para testes manuais) — não são escopo desta história.
- Files: `client/src/pages/UploadPage.tsx` (reescrito), `client/src/api/videos.ts` (adicionou `onProgress` + `onUploadProgress`), `client/src/index.css` (removidas `.upload-info/.upload-meta-summary/.upload-warning`, adicionado bloco `.upload-page/.upload-layout/.upload-dropzone/.upload-checklist/.upload-hint/.upload-progress/.upload-submit`).
- **Learnings for future iterations:**
  - Para upload com progresso real no axios, passe `onUploadProgress` no config da requisição (3º argumento do `api.post`): `onUploadProgress: (event) => { const total = event.total ?? event.loaded; const percent = Math.round((event.loaded/total)*100); ... }`. `event.total` pode ser `undefined` em alguns browsers — fallback para `event.loaded` e clamp 0–100.
  - Animação da barra de progresso: usar `transform: scaleX(percent/100)` com `transform-origin: left center` (compositor-friendly) em vez de `width: X%`. Proibido animar `width`/`height` por instrução da US-037 (e do padrão geral do redesign).
  - Preview de vídeo sem canvas: basta um `<video src={URL.createObjectURL(file)} preload="metadata" muted playsInline controls/>` — o browser já renderiza o primeiro frame (pôster implícito). Reutilize o mesmo object URL do arquivo, **não** crie outro dentro de `readVideoMetadata` (esse já revoga o seu próprio). Revogue o preview URL no `useEffect` cleanup e em cada `processFile` (capture o URL anterior em variável local antes de `setState` para evitar revogar o novo).
  - Dropzone acessível: `role="button"` + `tabIndex={0}` + `aria-label` + handler `onKeyDown` para Enter/Space. O `<input type="file">` fica escondido (posição absoluta clipada) e é disparado via `ref.current?.click()`. Um `onClick` no container e `preventDefault` nos handlers de drag (`onDragOver/onDragEnter/onDragLeave/onDrop`) evita que o browser abra o arquivo dropado numa nova aba.
  - `details/summary` colapsável com chevron: zere `list-style: none` e `::-webkit-details-marker { display:none }`, coloque `content: '+'` no `summary::after` e `content: '−'` no `[open] summary::after`. Padrão reutilizável para qualquer FAQ/hint futuro.
  - Pré-existente: `videoValidation.ts` tem `VIDEO_MIN_DURATION_SEC=6` e `VIDEO_MIN_FPS=20` (commit `98ca1eb config`) em vez dos 30/60 do PRD. A UI da US-037 **consome as constantes** (não hard-coded) para que, quando o usuário restaurar os valores, o checklist reflita automaticamente.
---

## [2026-04-22 20:55] - US-038 Redesign — tela de Status (progresso visual + tempo estimado)
- Reescrita `StatusPage.tsx` com hero (SVG runner animado via novo componente `RunnerHero`), barra de progresso `transform: scaleX`, stepper de 4 etapas (vertical em mobile, horizontal em ≥640px via `::before` conector), info de tempo decorrido (`formatElapsedTime`) e estimativa restante (`estimatedRemainingLabel`), estado de erro como card vermelho com Banner danger + dois botões (Enviar novo vídeo → `/upload`, Ver dicas de gravação → `/upload#dicas`), e estado celebratório com `CelebrationPanel` + redirect automático em 1.5s (novo `useEffect` com `setTimeout`) ou botão Ver diagnóstico.
- Renomeados rótulos do stepper em `videoStatus.ts` (Validando vídeo / Detectando pose / Calculando métricas / Gerando recomendações); adicionados helpers `formatElapsedTime(seconds)` e `estimatedRemainingLabel(elapsed)` com 6 testes novos (vitest). Adicionado `id="dicas"` no `<details>` da UploadPage para o anchor do botão "Ver dicas de gravação" scrollar até o bloco.
- Files: `client/src/pages/StatusPage.tsx` (reescrito), `client/src/components/RunnerHero.tsx` (novo), `client/src/utils/videoStatus.ts` (rótulos + helpers), `client/src/utils/videoStatus.test.ts` (novos testes), `client/src/index.css` (bloco `.status-*` completo reescrito com hero/stepper/progress/celebration + keyframes `runner-motion-pulse`/`runner-limb-swing`/`status-stepper-pulse`), `client/src/pages/UploadPage.tsx` (`id="dicas"` no `<details>`).
- **Learnings for future iterations:**
  - Animações SVG em elementos filhos precisam de `transform-box: fill-box; transform-origin: ...` no CSS para que `transform: rotate()/translateX()` se oriente pelo bounding-box do próprio path, não pelo viewBox do SVG. Sem `transform-box`, `transform-origin: left center` não faz o que você quer em um `<line>`/`<path>`.
  - Custom properties CSS (`--runner-speed`) controlam duração de animação de forma reativa: `animation: x var(--runner-speed, 1.2s) ...` + `calc(var(--runner-speed) * -0.3)` para `animation-delay` negativo dá staggering automático. Mude a var via `style={{ '--runner-speed': '0.8s' }}` no JSX (precisa cast `'--runner-speed' as string` para o objeto style por causa do TS).
  - Padrão "celebration → redirect": separe o redirect do `poll()`. O `poll` só marca `isFinalStatus(data.status) ⇒ cancelledRef = true` para parar o polling. Um `useEffect` separado observa `isCelebrating && videoId` e agenda `setTimeout(() => navigate(...), 1500)` com cleanup do timer no unmount — assim o usuário vê a UI celebratória por 1.5s antes da navegação, e o botão "Ver diagnóstico" pode fazer override imediato.
  - Para Button visualmente encadeado com navegação, NÃO aninhe `<Link><Button/></Link>` (HTML inválido — `<a>` contendo `<button>`). Use `<Button onClick={() => navigate(to)}>` com `useNavigate()`. A exceção é `ErrorState.backTo` que já renderiza um `<Link>` estilizado como link, não botão.
  - `VideoStatusResponse.video_id` é `number` no contrato do backend; o param da URL `:id` via `useParams` é `string | undefined`. Ao interpolar em rotas, converta com `String(data.video_id)` para unificar tipo (`string | null`).
---

## [2026-04-22 21:02] - US-039 Redesign — tela de Diagnóstico simplificado (hero da nota + cards visuais)
- Reescrita `AnalysisPage.tsx` com hero (anel SVG + classificação + feedback), grid 1/2-col de cards de métrica com ícones anatômicos, grupos de recomendação com `<h3>` destacado e cards semânticos, Banner verde no estado vazio e footer com dois botões (`Voltar ao histórico` + `Ver dados técnicos`). Título da página agora é `Diagnóstico #{id} — Stride`.
- Novos componentes em `client/src/components/analysis/`: `NotaRing.tsx` (anel circular SVG com cor dinâmica por faixa da nota — verde >=7, âmbar 5–7, vermelho <5) e `MetricIcon.tsx` (8 ícones anatômicos: joelho/cotovelo/tronco/passada/oscilação/simetria/cadência/tempo). `METRIC_CONFIGS` em `analysisDisplay.ts` ganhou campo `icon: MetricIconName` por entrada; novo helper `notaClassification(nota)` retorna 'Excelente'/'Bom'/'Atenção'/'—'.
- Atualizado `LoadingState` variant `analysis` para espelhar o novo layout (hero circular + grid 2-col de cards); CSS `.analysis-*` reescrito (hero, ring, metric-grid, severity-title com ícone, footer responsivo) mantendo os tokens e classes `.analysis-card*` compatíveis. 2 testes novos em `analysisDisplay.test.ts` (notaClassification). `npm run build` passa (692 KB bundle, pré-existente); 96/96 testes de `analysisDisplay` passam; as 2 falhas de `videoValidation.test.ts` são pré-existentes a esta história.
- Files: `client/src/pages/AnalysisPage.tsx`, `client/src/components/analysis/NotaRing.tsx` (novo), `client/src/components/analysis/MetricIcon.tsx` (novo), `client/src/utils/analysisDisplay.ts` (+notaClassification, +icon em cada config, +MetricIconName), `client/src/utils/analysisDisplay.test.ts`, `client/src/components/ui/states/LoadingState.tsx`, `client/src/index.css`.
- **Learnings for future iterations:**
  - Anel SVG de progresso em arco: `strokeDasharray={`${dashLength} ${dashGap}`}` + `transform="rotate(-90 cx cy)"` (começa no topo) + `strokeLinecap="round"`. `CIRCUMFERENCE = 2π·r` como constante; `dashLength = CIRCUMFERENCE * fraction`. Reutilizável para qualquer ring chart futuro (métrica de sessão, scores).
  - Ícones anatômicos/semânticos consomem `currentColor` + `stroke-linecap="round"` + `strokeWidth` constante (1.8) — usar `MetricIcon.tsx` como biblioteca base; adicionar novos ícones como `case` em um único `switch` para manter shape/sizing consistentes. Viewbox `0 0 24 24` é o padrão; valor `currentColor` permite a cor vir do container (`.analysis-metric-card-icon { color: var(--brand-600) }`).
  - Layouts "hero horizontal em desktop, empilhado em mobile" seguem o pattern: `.container { display: flex; flex-direction: column; gap; padding }` + `@media (min-width: 640px) { flex-direction: row }`. Use tokens `--space-*` para padding/gap (nada hard-coded). O mesmo pattern serve para o footer com 2 botões (`.analysis-footer`).
  - `notaClassName` (mapeamento nota→classe de cor para cards) e `notaClassification` (mapeamento nota→rótulo textual) usam limiares diferentes por design: notaClassName tem a faixa 4–7 como `nota-ok` (mantém retrocompatibilidade com o estilo de cartões existente), enquanto notaClassification trata <5 como 'Atenção' para casar com a faixa do anel (PRD US-039: verde >=7, âmbar 5–7, vermelho <5).
  - Classes `.analysis-feedback` e `.analysis-actions`/`.analysis-raw-link` ainda são consumidas pela `AnalysisRawPage` — mantê-las no CSS até a US-040 redesenhar essa tela. Não remova sem grep no frontend inteiro.
---

## [2026-04-22 21:12] - US-040 Redesign — tela de Dados brutos (charts profissionais + filtros)
- Reescrita `AnalysisRawPage.tsx` com paleta color-blind-friendly Okabe-Ito (esq = `#009E73` bluish green, dir = `#D55E00` vermillion, tronco = `#0072B2` blue, Y CoM = `#CC79A7` reddish purple). Charts principais (joelho, cotovelo, tronco) já eram overlay; agora usam cores Okabe-Ito, tooltip com `contentStyle` custom, altura 260px e margens otimizadas.
- Novo filtro de métrica em chips clicáveis (`role="tablist"`, `aria-selected`) encima do grid — reutiliza `<Badge>` da biblioteca UI (success ativo / neutral inativo). Opções: Todos, Joelho, Cotovelo, Tronco, Y CoM. Filtro "Todos" mostra grid 2-col em ≥900px; filtros individuais trocam para 1-col full-width via `.raw-chart-grid-single`.
- Nova tabela densa abaixo dos charts: paginada client-side (`ROWS_PER_PAGE=25`) com `useDeferredValue(page)` para desacoplar UI do recálculo do slice, destaque de linhas fora do ideal (background âmbar) e destaque de células críticas (cor `--severity-critico-text`). Botão **Exportar CSV** gera blob via `URL.createObjectURL` sem chamada à API; nome `stride-analise-{id}-frames.csv`. Header do CSV usa convenção visual (flexão, não ângulo interno) alinhada à tabela.
- `rawAnalysis.ts` ganhou: constantes `OKABE_ITO`, `FAIXAS_IDEAIS` (replicam PRD Seção 9), helpers `isValueOutOfRange`/`isFrameOutOfRange`, `buildCsv`/`buildCsvFilename`/`CSV_COLUMNS`, e `chartKey`/`shortLabel` em `ChartSpec` (cada spec agora identificável). Testes novos em `rawAnalysis.test.ts` (21 novos casos: faixa ideal joelho/cotovelo/tronco, NaN, CSV header+células vazias, filename).
- Files: `client/src/pages/AnalysisRawPage.tsx`, `client/src/utils/rawAnalysis.ts`, `client/src/utils/rawAnalysis.test.ts`, `client/src/index.css`.
- `npm run build` passa (698 KB bundle, pré-existente — nota de warning do rolldown está anterior à história); 30/30 testes de `rawAnalysis` passam; as 2 falhas em `videoValidation.test.ts` permanecem pré-existentes.
- **Learnings for future iterations:**
  - Paleta Okabe-Ito (`client/src/utils/rawAnalysis.ts#OKABE_ITO`) é a fonte única para quaisquer novos charts/visualizações que precisem codificar esq vs dir ou múltiplas séries. Preserva contraste em deuteranopia/protanopia + escala de cinza. Reuse esta constante em vez de repetir hex literais.
  - Filtros por chips com semântica de tabs: `<div role="tablist"><button role="tab" aria-selected>`. Visual via `<Badge variant="success|neutral">` trocado por estado ativo — barato, acessível e 100% consistente com a biblioteca UI (não precisa de componente `Tab` dedicado).
  - Tabela densa com pagination client-side + `useDeferredValue`: `deferredPage = useDeferredValue(page)` e use `deferredPage` para computar o slice das rows (`points.slice(...)`). O input imediato (clique de botão) atualiza `page`, o React renderiza a nova página como low-priority — perceptível em datasets grandes (>1k frames). Reusável para US-041 (histórico) e futuras tabelas.
  - Export CSV client-side pattern: `new Blob([csv], {type: 'text/csv;charset=utf-8'})` → `URL.createObjectURL` → âncora `download` → `URL.revokeObjectURL`. Criar âncora invisível, clicar, remover no mesmo tick é o approach mais compatível (dispensa lib externa). `buildCsv` é função pura testável em Node.
  - Faixas ideais do PRD (`FAIXAS_IDEAIS` em `rawAnalysis.ts`) duplicam os limiares de `server/src/biomechanics/recomendacoes.py#_classificar_*`. Manter sincronia manual — se o backend mudar os thresholds, atualizar `FAIXAS_IDEAIS` também. Vale reforçar isso com teste end-to-end quando US-043 instalar integração de Lighthouse/e2e.
---

## [2026-04-22 21:25] - US-041 Redesign — tela de Histórico (mini-sparklines + filtros)
- Reescrita `HistoricoPage.tsx` com hero (média da página + sparkline SVG + delta vs sessões anteriores), grupo de filtros por chips (período `7d`/`30d`/`todos` + status `concluído`/`erro`/`todos`) deep-linkados na query string, lista de sessões em **cards** (1-col mobile, 2-col ≥768px) substituindo a antiga tabela em grid e paginação acessível com `<Button variant="ghost"><nav aria-label="Paginação">`. Cards `concluido` clicáveis → `/analysis/:id`; cards de erro mantêm motivo expansível.
- Novo componente `client/src/components/historico/Sparkline.tsx` (SVG inline puro, sem Recharts, escala fixa [0,10] na nota geral) consumido pelo hero. Mantém o bundle do histórico leve mesmo se Recharts virar lazy em US-043.
- `historicoDisplay.ts` ganhou: filtros pt-BR (`PERIODO_*`, `FILTRO_STATUS_*`, `parsePeriodoParam`/`parseStatusFilterParam`, `matchesPeriodo`/`matchesStatusFilter`/`filterItems`), helpers de evolução (`sparklinePoints`, `averageNota`, `notaTrendDelta`, `formatNotaAverage`, `formatTrendDelta`, `trendDirection`) e `formatCriadoEm` migrado para `Intl.DateTimeFormat('pt-BR')` (formatters separados de data/hora cacheados no module-level). `LoadingState variant="historico"` redesenhado para espelhar o novo layout (hero card + filtros + grid de cards).
- Restauração de scroll back/forward via `sessionStorage['historico:scroll']`: salva `window.scrollY` antes de `navigate(detalhe)` no clique do card, restaura no mount após `loading=false`. `useRef(false)` previne restore repetido em re-renders subsequentes.
- 23 testes novos em `historicoDisplay.test.ts` cobrindo filtros, sparkline ordering, average/trend e formatters de média/delta. Total: 35/35 passam. `npm run build` passa (706 KB bundle, pré-existente). Falhas em `videoValidation.test.ts` continuam pré-existentes a esta história (FPS warning + duração curta — não tocadas).
- Files: `client/src/pages/HistoricoPage.tsx`, `client/src/components/historico/Sparkline.tsx` (novo), `client/src/utils/historicoDisplay.ts`, `client/src/utils/historicoDisplay.test.ts`, `client/src/components/ui/states/LoadingState.tsx`, `client/src/index.css`.
- **Learnings for future iterations:**
  - URL canônica de filtros: nunca persistir o valor default (`todos`) — `nextParams.delete('chave')` em vez de `set('chave', 'todos')` mantém a URL curta e o histórico do browser limpo. O parser (`parsePeriodoParam` etc.) trata `null` como default; o helper `filterItems` é seguro de chamar mesmo com defaults.
  - Reset de `page` ao trocar filtro: `nextParams.delete('page')` (em vez de `set('page', '1')`) deixa a URL canônica idêntica à URL inicial. Evita confusão se o usuário vier de um deep-link e ajustar filtros — back ainda funciona.
  - `Intl.DateTimeFormat('pt-BR')` com `{dateStyle, timeStyle}` adiciona vírgula entre data e hora em algumas versões do engine (`'21/04/2026, 14:05'`). Separe em dois formatters (apenas date / apenas time) e concatene com espaço para o shape `dd/mm/yyyy HH:MM` permanecer estável. Os formatters podem ser declarados no module-level para evitar realocação por chamada.
  - Filtragem **client-side sobre a página atual**: o backend `GET /api/historico-analise` não aceita filtros, então as facetas operam só sobre `data.items` da página. UI honesta: se a página atual fica vazia após filtros, exiba `EmptyState` específico ("Nenhuma análise nesta página com os filtros atuais") com CTA "Limpar filtros" — não confunda com o `EmptyState` "sem análises ainda" (esse só aparece quando `data.items.length === 0` no backend).
  - Stat de tendência usa **todos** os itens da página (não só os filtrados) para que o número não dance ao alternar facetas. Trade-off deliberado: o stat reflete "o que esta página mostra como evolução", não "o que o filtro mostra agora". Documentado via label "Média desta página".
---

## [2026-04-22 21:31] - US-042 Microinterações e motion coerentes
- Adicionou hover `translateY(-1px) + shadow-md` e active `translateY(0) + shadow-xs` em todos os `.ui-button` (variantes ghost mantêm shadow neutro); spinner do Button virou SVG inline (`<circle>` track + `<path>` arc) com `@keyframes ui-spin` rotação pura — `prefers-reduced-motion` global neutraliza.
- Criou `.route-fade` (fade 200ms em wrapper keyado por `location.pathname` em `App.tsx#RoutedApp`) e `.list-enter` (opacity+translateY 280ms, escalonado via `--enter-index` inline). Aplicado a cards do Histórico (`HistoricoCard`) e às recomendações em `AnalysisPage`.
- Removeu única violação `transition: width` em `.raw-asymmetry-bar-fill` (bar é set-once via inline style, não anima de fato — declaração era ruído). Audit: zero `transition: all`, zero animações de `width/height/top/left/right/bottom` no CSS.
- Files: `client/src/index.css`, `client/src/components/ui/Button.tsx`, `client/src/App.tsx`, `client/src/pages/HistoricoPage.tsx`, `client/src/pages/AnalysisPage.tsx`.
- **Learnings for future iterations:**
  - Para route-fade interruptível em React Router v6 sem nada de adicional: extraia um componente filho de `BrowserRouter` que chama `useLocation()` e envolve `<Routes location={location}>` num `<div className="route-fade" key={location.pathname}>`. O remount do wrapper a cada navegação reinicia o `@keyframes`, e o navegação encadeada (back/forward rápida) cancela a animação anterior naturalmente. Trade-off: páginas remontam ao trocar de rota, perdendo state local da página — aceitável já que cada página normalmente refaz fetch no mount.
  - Stagger de entrada de listas sem JS: classe `.list-enter` + custom property `--enter-index` na inline style do item. CSS resolve `animation-delay: calc(var(--enter-index, 0) * 30ms)`. TypeScript precisa de cast em `style={{ ['--enter-index' as string]: idx }}` porque `CSSProperties` não conhece custom props. CSS animations só disparam quando o elemento é criado/recriado — re-renders não retriggeram, satisfaz "somente na primeira renderização".
  - Hover/active microinteractions devem ser declaradas no `.ui-button` base (com `:not(:disabled):not([aria-busy='true'])`) e desabilitadas seletivamente para variantes que conflitam (e.g. `ghost` precisa `box-shadow: none` em todos os estados, senão fica com sombra "flutuante" sobre fundo transparente). Padrão reutilizável: base aplica todos os estados, variantes só fazem override seletivo.
  - O reset global `prefers-reduced-motion` em `index.css` neutraliza `transition-duration`/`animation-duration` em `*`, `*::before`, `*::after`. Animações novas NÃO precisam repetir o media query — basta usar apenas `opacity`/`transform`. Para spinner de loading que ainda gira em reduced-motion (informativo de progresso), o reset zera mesmo assim — comportamento aceitável já que o `aria-busy` no `<button>` carrega a semântica de "ocupado" para leitores de tela.
---

## [2026-04-22 21:38] - US-043 Performance e bundle — lazy routes, preload e image dims
- Converteu todas as rotas privadas (Upload, Status, Analysis, AnalysisRaw, Historico) para `React.lazy()` + `<Suspense fallback={<LoadingState variant=...>}>` em `client/src/App.tsx`; Login/Signup permanecem em import estático para servir a landing sem round-trip de chunk. Helper `privateElement(node, variant)` centraliza o `PrivateRoute + AppShell + Suspense` boilerplate.
- Bundle inicial caiu de **214.55 KB → 97.34 KB gzipped** (index principal). AnalysisRawPage (com Recharts) virou chunk separado `AnalysisRawPage-*.js` ~106 KB gzipped — só baixa quando o usuário navega para `/analysis/:id/raw`. Demais rotas em 2.40–4.57 KB gz por chunk.
- Instrumentou timeouts em `client/src/api/client.ts`: `DEFAULT_TIMEOUT_MS=15000` na instância axios + request interceptor que aplica `UPLOAD_TIMEOUT_MS=30000` para `/api/videos/upload` e `AUTH_TIMEOUT_MS=10000` para `/api/auth/login` e `/api/users/register`. Preconnect à origem da API (quando `VITE_API_BASE_URL` ≠ same-origin) injetado em runtime por `preconnectApiOrigin()` em `main.tsx` — best-effort, try/catch silencioso em URL inválida.
- Auditoria de imagens: codebase não tem `<img>` — todas as ilustrações são SVG inline (Logo, AppShell, hero gradients) e não sofrem CLS; critérios de `width/height` e `loading=lazy` são vacuously satisfied. Fontes continuam com `<link rel=preload as=font crossorigin>` em `index.html` (instalado em US-028).
- Files: `client/src/App.tsx`, `client/src/main.tsx`, `client/src/api/client.ts`, `client/CLAUDE.md`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Rotas lazy exigem que cada `<Suspense fallback={...}>` escolha uma `LoadingVariant` que **aproxime** o layout da página durante o download do chunk (~50–200ms numa rede típica). Para rotas sem variant dedicada (UploadPage), usar `'status'` é aceitável — um placeholder pequeno evita "tela branca". Se uma nova rota divergir muito das 4 variants existentes, acrescente uma nova variant em `components/ui/states/LoadingState.tsx` em vez de reusar uma mal-dimensionada.
  - Recharts só é usado em `AnalysisRawPage` hoje; lazy-loading a página já isola ~110 KB gzipped do bundle inicial. Se qualquer rota futura (ex: dashboards) também usar Recharts, NÃO importe estaticamente — use `const Chart = lazy(() => import('./components/ChartSerie'))` ou extraia o componente que consome Recharts para um arquivo próprio e chame `lazy()` ali. A alternativa (`manualChunks: { recharts: ['recharts'] }` em `vite.config.ts`) cria um chunk shared que é baixado no primeiro entrypoint que tocar recharts — evite enquanto a feature for raramente visitada.
  - Para preconnect dinâmico dependente de env var (o `VITE_API_BASE_URL` não está disponível em `index.html` sem HTML template plugin), injete o `<link rel=preconnect>` via JS no topo de `main.tsx` **antes** do `createRoot(...).render(...)`. Guard com `try/catch` em `new URL(...)` e checagem `apiOrigin === window.location.origin` para evitar preconnect redundante quando o API é same-origin (dev via proxy do Vite).
  - Timeouts por-endpoint em axios: configurar um `DEFAULT_TIMEOUT_MS` no `axios.create({ timeout })` e sobrescrever no request interceptor inspecionando `config.url`. Convenção: `/upload` → 30s (uploads grandes em redes lentas); auth (`/login`, `/register`) → 10s (sinalizar rede ruim rapidamente ao invés de congelar); default → 15s. Chamadas GET podem passar `timeout` explícito para override pontual.
---
